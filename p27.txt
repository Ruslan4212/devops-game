W("linux").miss.push(

{id:"l6",t:"systemd: сервисы навсегда",goal:"Сделать так, чтобы приложение поднималось само",
 cards:[
 {h:"Зачем нужен systemd",b:"<p>Приложение, запущенное командой в терминале, умирает вместе с сессией. Приложение, запущенное через <code>nohup</code> в фоне, переживает выход, но не переживает перезагрузку, не перезапускается после падения и не имеет внятного журнала.</p><p><b>systemd</b> — система инициализации и менеджер служб. Он решает четыре задачи сразу: запуск при загрузке, перезапуск после падения, порядок и зависимости между сервисами, сбор журналов.</p><p>Единица управления называется <b>юнит</b>. Типы юнитов: <code>.service</code> (служба), <code>.timer</code> (расписание), <code>.socket</code> (активация по обращению к порту), <code>.target</code> (группа юнитов, аналог уровня запуска), <code>.mount</code> (точка монтирования).</p>"},
 {h:"Юнит-файл целиком",b:"<pre>[Unit]\nDescription=Shop API\nAfter=network-online.target postgresql.service\nRequires=postgresql.service\n\n[Service]\nType=simple\nUser=app\nGroup=app\nWorkingDirectory=/opt/app\nEnvironmentFile=/etc/app/env\nExecStart=/opt/app/bin/api --port 8080\nExecReload=/bin/kill -HUP $MAINPID\nRestart=on-failure\nRestartSec=5\nLimitNOFILE=65535\n\n[Install]\nWantedBy=multi-user.target</pre><div class='tw'><table><tr><th>секция</th><th>отвечает за</th></tr><tr><td><b>[Unit]</b></td><td>описание и зависимости от других юнитов</td></tr><tr><td><b>[Service]</b></td><td>как именно запускать процесс</td></tr><tr><td><b>[Install]</b></td><td>куда цеплять юнит при включении автозапуска</td></tr></table></div><p>Свои юниты кладут в <code>/etc/systemd/system/имя.service</code>. Юниты из пакетов лежат в <code>/lib/systemd/system/</code> — их не редактируют напрямую, потому что обновление пакета затрёт правки. Вместо этого используют <code>systemctl edit имя</code>, который создаёт файл-дополнение.</p>"},
 {h:"After, Requires, Wants — три разные вещи",b:"<div class='tw'><table><tr><th>директива</th><th>что задаёт</th><th>что будет, если зависимость упадёт</th></tr><tr><td><code>After=</code></td><td>только <b>порядок</b> запуска</td><td>ничего: наш сервис продолжит работать</td></tr><tr><td><code>Requires=</code></td><td>жёсткую <b>зависимость</b></td><td>наш сервис тоже остановится</td></tr><tr><td><code>Wants=</code></td><td>мягкую зависимость</td><td>ничего, запуск просто попытаются выполнить</td></tr></table></div><p><b>Ключевая тонкость:</b> <code>Requires</code> <i>не задаёт порядок</i>. Указав только его, ты получишь параллельный запуск обоих юнитов — и приложение попытается подключиться к ещё не готовой базе.</p><p>Поэтому на практике всегда пишут обе строки: <code>Requires=postgresql.service</code> и <code>After=postgresql.service</code>.</p><p><b>И вторая тонкость:</b> даже правильный порядок не гарантирует готовность. systemd считает сервис запущенным, когда стартовал процесс, а не когда база начала принимать соединения. Надёжное решение — ретраи подключения в самом приложении.</p>"},
 {h:"Type: чем simple отличается от forking",b:"<div class='tw'><table><tr><th>Type</th><th>когда использовать</th></tr><tr><td><b>simple</b></td><td>процесс работает на переднем плане (большинство современных приложений)</td></tr><tr><td><b>forking</b></td><td>программа сама уходит в фон и порождает демона (классические демоны)</td></tr><tr><td><b>oneshot</b></td><td>задача выполняется и завершается (миграции, скрипты)</td></tr><tr><td><b>notify</b></td><td>приложение само сообщает systemd о готовности</td></tr></table></div><p>Неверный Type — частая причина странного поведения: при <code>simple</code> для форкающегося демона systemd считает сервис упавшим сразу после старта, при <code>forking</code> для обычного приложения — зависает в ожидании ухода в фон.</p><p>Правило: если ты не запускаешь классический демон из нулевых, тебе нужен <code>simple</code>.</p>"},
 {h:"start против enable",b:"<pre>systemctl start api        <span class='c'># запустить прямо сейчас</span>\nsystemctl enable api       <span class='c'># запускать при загрузке системы</span>\nsystemctl enable --now api <span class='c'># и то, и другое</span>\nsystemctl is-enabled api\nsystemctl is-active api\nsystemctl list-units --failed   <span class='c'># что сломано прямо сейчас</span></pre><p>Это <b>две разные операции</b>. <code>enable</code> создаёт символическую ссылку в каталоге <code>multi-user.target.wants</code> — именно она заставляет юнит стартовать при загрузке.</p><p><b>Классика жанра:</b> всё работало неделю, сервер перезагрузили — сервис не поднялся, потому что забыли <code>enable</code>. Проверить одной командой: <code>systemctl is-enabled api</code>.</p><p><code>systemctl list-units --failed</code> стоит выполнять первым делом на незнакомом сервере: он сразу показывает всё, что не смогло запуститься.</p>"},
 {h:"Restart и как не устроить шторм",b:"<div class='tw'><table><tr><th>значение</th><th>когда перезапускать</th></tr><tr><td><code>no</code></td><td>никогда (по умолчанию)</td></tr><tr><td><code>on-failure</code></td><td>только при ненулевом коде выхода — обычный выбор</td></tr><tr><td><code>always</code></td><td>всегда, даже после штатной остановки</td></tr><tr><td><code>on-abnormal</code></td><td>при сигнале или таймауте</td></tr></table></div><pre>Restart=on-failure\nRestartSec=5\nStartLimitBurst=5\nStartLimitIntervalSec=60</pre><p><code>RestartSec</code> — пауза между попытками. Без неё падающий сервис перезапускается сотни раз в минуту, забивает журнал и нагружает диск.</p><p><code>StartLimitBurst</code> и <code>StartLimitIntervalSec</code> ограничивают число попыток в окне: после пяти неудач за минуту systemd прекращает попытки и оставляет сервис в состоянии failed. Это правильно: бесконечный перезапуск маскирует проблему вместо того, чтобы показать её.</p><p>Важно и обратное: <code>Restart=always</code> с коротким RestartSec на сервисе с ошибкой в конфиге способен за ночь заполнить диск журналом.</p>"},
 {h:"Диагностика и главная ловушка",b:"<pre>systemctl status api               <span class='c'># состояние, PID, память, последние строки лога</span>\njournalctl -u api -f               <span class='c'># живой журнал сервиса</span>\njournalctl -u api --since \"10 min ago\"\njournalctl -u api -p err -b        <span class='c'># только ошибки текущей загрузки</span>\nsystemctl cat api                  <span class='c'># итоговый юнит со всеми дополнениями</span>\nsystemctl show api -p Restart      <span class='c'># значение конкретного параметра</span></pre><p><b>Ловушка, на которую попадаются все:</b> systemd держит юниты в памяти. Ты правишь файл, делаешь <code>systemctl restart</code> — и ничего не меняется, потому что перечитан старый юнит из кэша.</p><p>Порядок всегда один: <b>правка файла → <code>systemctl daemon-reload</code> → <code>restart</code> → проверка <code>status</code></b>.</p><p>Подсказка, что ты попал в эту ловушку: в выводе <code>status</code> видна прежняя команда запуска, хотя в файле она другая. И сам systemd в этом случае обычно пишет предупреждение «Warning: The unit file changed on disk».</p>"},
 {h:"reload против restart",b:"<pre>systemctl reload nginx    <span class='c'># сервис перечитает конфиг без разрыва соединений</span>\nsystemctl restart nginx   <span class='c'># процесс останавливается и запускается заново</span>\nsystemctl reload-or-restart nginx</pre><p><code>reload</code> работает только если в юните описано, что именно делать: <code>ExecReload=</code>. Для nginx это отправка сигнала, по которому он поднимает новые рабочие процессы с новым конфигом, а старые дорабатывают текущие запросы.</p><p>Практическое правило для веб-сервера: <b>сначала проверка конфига, потом reload</b>. <code>nginx -t &amp;&amp; systemctl reload nginx</code> — одна строка, которая экономит продакшены: при ошибке в конфиге reload не выполнится, а restart оставил бы тебя без веб-сервера.</p>"}],
 ch:[
 {t:"c",d:1,q:"Ты подключился к серверу и хочешь понять, работает ли nginx, когда он последний раз стартовал и что писал в журнал. Одна команда.",ans:["systemctl status nginx","sudo systemctl status nginx"],hint:"systemctl и подкоманда состояния",why:"status показывает состояние, PID, потребление памяти, время старта и последние строки журнала — максимум информации за одну команду."},
 {t:"q",d:2,q:"Сервер перезагрузили после планового обслуживания, и приложение не поднялось, хотя две недели до этого работало без нареканий. <code>systemctl start api</code> запускает его нормально. Что было упущено при настройке?",a:["Не хватило прав у сервисного пользователя","Не выполнили <code>systemctl enable api</code> — сервис работал, но не был включён в автозапуск","Юнит-файл повреждён","Не задан Restart=always"],c:1,why:"start и enable — разные операции: первая запускает сейчас, вторая создаёт ссылку, заставляющую юнит стартовать при загрузке. Проверяется командой systemctl is-enabled."},
 {t:"c",d:2,q:"Ты отредактировал файл /etc/systemd/system/api.service. Какой командой заставить systemd перечитать юниты перед перезапуском сервиса?",ans:["systemctl daemon-reload","sudo systemctl daemon-reload"],hint:"daemon-...",why:"systemd кэширует юниты в памяти. Без daemon-reload перезапуск поднимет сервис по старой конфигурации, и ты будешь искать несуществующий баг, глядя на правильный файл."},
 {t:"q",d:3,q:"В юните приложения указано <code>Requires=postgresql.service</code>, но при загрузке сервера приложение всё равно падает с «connection refused» к базе. Чего не хватает и почему одной этой строки мало?",a:["Нужно добавить Restart=always","Нужна ещё директива <code>After=postgresql.service</code>: Requires задаёт зависимость, но не порядок запуска — без After оба юнита стартуют параллельно","Нужно увеличить RestartSec","Нужно указать Type=forking"],c:1,why:"Requires и After решают разные задачи, поэтому на практике пишут обе строки. И даже это не гарантирует готовность базы принимать соединения — надёжное решение включает ретраи подключения в приложении."},
 {t:"q",d:4,q:"Сервис падает из-за ошибки в конфигурации, а в юните стоит <code>Restart=always</code> без <code>RestartSec</code> и без ограничений на число попыток. Что произойдёт с сервером за ночь?",a:["Ничего страшного: сервис просто не будет работать","Шторм перезапусков: сотни попыток в минуту нагрузят диск и заполнят журнал, возможно, до переполнения раздела — нужны RestartSec и StartLimitBurst","systemd сам остановит попытки через минуту","Сервер перезагрузится автоматически"],c:1,why:"Перезапуск должен давать время починить причину, а не маскировать её. Правильная связка: Restart=on-failure, RestartSec=5 и лимит попыток в окне, после которого сервис остаётся в состоянии failed и попадает в алерты."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Ты изменил путь в <code>ExecStart</code>, выполнил <code>systemctl restart api</code> — сервис поднялся, но работает по старому пути. В выводе <code>systemctl status api</code> видна прежняя команда запуска, хотя в файле она другая. Что произошло и какова правильная последовательность?",a:["Файл сохранён не в тот каталог, нужно проверить путь","systemd работает с кэшированной в памяти версией юнита: нужно выполнить <code>systemctl daemon-reload</code>, затем restart и проверить status — именно расхождение файла и вывода status и есть подсказка","Требуется перезагрузка сервера","Сервис запущен от другого пользователя"],c:1,why:"Вывод status показывает то, что systemd держит в памяти, — поэтому расхождение с содержимым файла однозначно указывает на пропущенный daemon-reload. systemd обычно и сам предупреждает строкой «unit file changed on disk»."}]},

{id:"l7",t:"Планировщик задач",goal:"Автоматизировать рутину по расписанию и узнавать о сбоях",
 cards:[
 {h:"cron: пять полей",b:"<pre>crontab -e        <span class='c'># редактировать задания текущего пользователя</span>\ncrontab -l        <span class='c'># посмотреть</span>\nsudo crontab -e -u deploy   <span class='c'># задания другого пользователя</span>\n\n<span class='c'># мин  час  день_месяца  месяц  день_недели  команда</span>\n0    3    *    *    *   /opt/scripts/backup.sh\n*/5  *    *    *    *   /opt/scripts/check.sh\n30   2    *    *    1   /opt/scripts/weekly.sh   <span class='c'># по понедельникам</span>\n0    */6  *    *    *   /opt/scripts/sync.sh     <span class='c'># каждые 6 часов</span></pre><p><code>*</code> — любое значение, <code>*/N</code> — каждые N единиц этого поля, <code>1-5</code> — диапазон, <code>1,15</code> — перечисление.</p><p><b>Первое поле — минуты</b>, поэтому <code>*/5 * * * *</code> означает «каждые пять минут», а не «каждые пять часов». Ошибка в позиции поля — самая частая при чтении чужого crontab.</p><p>Файлы системных заданий лежат в <code>/etc/cron.d/</code> — там формат отличается: между расписанием и командой добавляется имя пользователя.</p>"},
 {h:"Почему скрипт работает руками и молчит из cron",b:"<p>Три причины, в порядке частоты:</p><ol><li><b>Другое окружение.</b> У cron минимальный <code>PATH</code> (обычно <code>/usr/bin:/bin</code>) и нет твоих переменных из <code>.bashrc</code>. Команда, которая есть в <code>/usr/local/bin</code>, просто не найдётся.</li><li><b>Вывод никуда не идёт.</b> По умолчанию cron пытается отправить его почтой, которой на сервере нет. Ошибку никто не видит.</li><li><b>Нет прав или бита выполнения</b> у скрипта, либо он запускается не от того пользователя.</li></ol><pre>*/5 * * * * /opt/scripts/check.sh &gt;&gt; /var/log/check.log 2&gt;&amp;1</pre><p>Правило: в crontab пишутся <b>абсолютные пути</b> и обязательно перенаправление обоих потоков в файл. Иначе ты не узнаешь ни об успехе, ни о провале.</p><p>Проверить окружение cron можно прямым способом: временно поставить задание <code>* * * * * env &gt; /tmp/cron-env.txt</code> и сравнить с выводом <code>env</code> в своей сессии.</p>"},
 {h:"systemd timer — современная замена",b:"<pre># /etc/systemd/system/backup.service\n[Service]\nType=oneshot\nExecStart=/opt/scripts/backup.sh\n\n# /etc/systemd/system/backup.timer\n[Timer]\nOnCalendar=*-*-* 03:00:00\nPersistent=true\nRandomizedDelaySec=300\n\n[Install]\nWantedBy=timers.target</pre><pre>systemctl enable --now backup.timer\nsystemctl list-timers          <span class='c'># когда что сработает и когда работало</span>\njournalctl -u backup           <span class='c'># журнал самой задачи</span>\nsystemctl start backup.service <span class='c'># запустить вручную для проверки</span></pre><div class='tw'><table><tr><th>преимущество</th><th>что даёт на практике</th></tr><tr><td>журнал в journalctl</td><td>вывод сохраняется сам, без перенаправлений</td></tr><tr><td><code>Persistent=true</code></td><td>догоняет пропущенный запуск, если машина была выключена</td></tr><tr><td>зависимости от юнитов</td><td>задача не стартует раньше нужного сервиса</td></tr><tr><td>ручной запуск</td><td>можно проверить результат немедленно, не дожидаясь расписания</td></tr><tr><td><code>RandomizedDelaySec</code></td><td>размазывает нагрузку, если задача крутится на сотне машин</td></tr></table></div>"},
 {h:"Блокировка от параллельного запуска",b:"<p>Задача по расписанию обязана выдерживать ситуацию, когда предыдущий запуск ещё не закончился. Бэкап растущей базы однажды выйдет за интервал — и второй экземпляр начнёт писать в тот же файл.</p><pre>flock -n /var/lock/backup.lock /opt/scripts/backup.sh || exit 0</pre><p><code>flock -n</code> завершается сразу, если блокировка занята. Флаг <code>-n</code> здесь принципиален: без него второй запуск будет <i>ждать</i> освобождения, и к утру ты получишь очередь из шести ожидающих процессов.</p><p>Для systemd-таймеров задача решается сама: сервис типа oneshot не запустится повторно, пока предыдущий запуск не завершился.</p>"},
 {h:"Мониторинг самих заданий",b:"<p>Главная проблема фоновых задач — <b>тихий отказ</b>. Задание перестало выполняться месяц назад, и никто не заметил, потому что отсутствие результата ничем не проявляется.</p><p>Три способа узнавать:</p><ul><li><b>Проверять результат, а не запуск.</b> Алерт «свежесть последнего бэкапа больше 26 часов» надёжнее, чем «задание завершилось с кодом 0».</li><li><b>Dead man's switch.</b> Скрипт после успеха дёргает внешний сервис; если сигнал не пришёл вовремя, приходит алерт. Так ловится случай «сервер выключен целиком».</li><li><b>Экспорт метрик</b> из задания в мониторинг (время выполнения, размер результата).</li></ul><p>Логика простая: любая проверка, основанная на активном действии системы, не сработает, когда система мертва. Проверять надо факт наличия свежего результата.</p>"},
 {h:"Чек-лист задачи по расписанию",b:"<ul><li>абсолютные пути ко всем командам и файлам</li><li>перенаправление stdout и stderr в лог с отметками времени</li><li>блокировка от параллельного запуска</li><li>идемпотентность: повторный запуск не портит результат</li><li>ненулевой код возврата при ошибке — иначе мониторинг не узнает</li><li>алерт на отсутствие свежего результата, а не на факт запуска</li><li>ротация логов самой задачи</li></ul>"}],
 ch:[
 {t:"q",d:1,q:"В crontab коллеги строка <code>*/5 * * * * /opt/scripts/check.sh</code>. Как часто выполняется скрипт?",a:["Каждые 5 часов","Каждые 5 минут","Раз в 5 дней","Пять раз в сутки"],c:1,why:"Порядок полей: минуты, часы, день месяца, месяц, день недели. Первое поле — минуты, поэтому */5 в нём означает «каждые пять минут»."},
 {t:"c",d:2,q:"Открой редактор заданий cron для текущего пользователя, чтобы добавить новую задачу.",ans:["crontab -e"],hint:"crontab и флаг edit",why:"crontab -l только показывает список. Задания хранятся отдельно у каждого пользователя, поэтому важно понимать, от кого будет выполняться команда."},
 {t:"q",d:3,q:"Скрипт отлично отрабатывает при запуске руками, но из cron не делает ничего и не оставляет следов. Назови две наиболее вероятные причины и что сделать в первую очередь.",a:["Cron сломан, нужно перезапустить демон","Другое окружение (минимальный PATH, нет переменных из профиля) и отсутствие перенаправления вывода, из-за чего ошибка не видна: нужны абсолютные пути и <code>&gt;&gt; log 2&gt;&amp;1</code>","Не хватает памяти во время ночного запуска","Скрипт требует интерактивного ввода"],c:1,why:"Эти две причины покрывают подавляющее большинство случаев. Сначала добавь перенаправление — тогда ты увидишь настоящую ошибку вместо гадания. Проверить окружение можно заданием env > /tmp/cron-env.txt."},
 {t:"f",d:3,q:"Допиши хвост строки crontab так, чтобы и обычный вывод, и сообщения об ошибках дописывались в файл /var/log/check.log.",pre:"*/5 * * * * /opt/scripts/check.sh ___",ans:[">> /var/log/check.log 2>&1",">>/var/log/check.log 2>&1"],hint:"дописать в файл, затем привязать stderr к stdout",why:"Порядок обязателен: сначала перенаправление stdout в файл, потом 2>&1 привязывает stderr туда же. Обратный порядок оставит ошибки в никуда."},
 {t:"q",d:4,q:"Чем systemd timer практически лучше cron для боевого сервера? Назови наиболее весомое отличие.",a:["Более понятный синтаксис расписания","Журнал задачи попадает в journalctl автоматически, задачу можно запустить вручную и сразу увидеть результат, а Persistent=true догоняет пропущенный запуск после простоя машины","Работает без root","Потребляет меньше ресурсов"],c:1,why:"Возможность выполнить задачу как обычный сервис и тут же посмотреть журнал радикально ускоряет отладку: с cron приходится ждать расписания или вручную воспроизводить окружение."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Ночной бэкап базы шёл дольше обычного из-за роста данных. В 03:00 запустился следующий экземпляр поверх незавершённого, оба писали в один архив. Файл оказался повреждён, и это обнаружили через две недели при попытке восстановления. Какие три меры предотвратили бы это?",a:["Запускать бэкап реже и на более мощном сервере","Блокировка от параллельного запуска (<code>flock -n</code>), запись во временный файл с переименованием после успеха, и алерт на свежесть последнего успешного бэкапа вместо доверия к отсутствию ошибок","Увеличить размер диска и добавить памяти","Перенести задание на выходные"],c:1,why:"Три независимых барьера: параллельный запуск становится технически невозможен, повреждённый результат не заменяет предыдущий рабочий, а мониторинг проверяет наличие свежего пригодного бэкапа, а не факт запуска задания."}]},

{id:"l8",t:"Пакеты, пользователи, sudo",goal:"Управлять системой, а не только смотреть на неё",
 cards:[
 {h:"Пакетные менеджеры",b:"<pre>apt update                  <span class='c'># обновить индекс доступных пакетов</span>\napt upgrade                 <span class='c'># обновить установленное</span>\napt install -y nginx\napt remove nginx            <span class='c'># удалить, конфиги в /etc остаются</span>\napt purge nginx             <span class='c'># удалить вместе с конфигами</span>\napt list --installed | grep nginx\napt-cache policy nginx      <span class='c'># какая версия установлена и что доступно</span></pre><div class='tw'><table><tr><th>семейство</th><th>менеджер</th><th>особенности</th></tr><tr><td>Debian / Ubuntu</td><td><code>apt</code>, <code>dpkg</code></td><td>чаще всего на серверах и в образах</td></tr><tr><td>RHEL / Rocky / Fedora</td><td><code>dnf</code>, <code>yum</code>, <code>rpm</code></td><td>корпоративные среды, SELinux включён по умолчанию</td></tr><tr><td>Alpine</td><td><code>apk</code></td><td>внутри контейнеров, минимальный размер</td></tr></table></div><p><b>Всегда <code>apt update</code> перед install.</b> Без него используется устаревший индекс: пакет «не находится» или ставится версия с уже исправленной уязвимостью. В Dockerfile эти команды объединяют в одну инструкцию RUN, чтобы кэш слоя не сохранил старый индекс.</p>"},
 {h:"Пользователи, UID и группы",b:"<pre>adduser deploy               <span class='c'># создать с домашним каталогом (Debian)</span>\nusermod -aG sudo deploy      <span class='c'># добавить в группу</span>\ngroups deploy                <span class='c'># в каких группах состоит</span>\nid deploy                    <span class='c'># uid, gid и все группы</span>\nsu - deploy                  <span class='c'># переключиться (дефис даёт полное окружение)</span>\ncat /etc/passwd              <span class='c'># список пользователей</span></pre><p>Внутри системы пользователь — это <b>число</b> (UID), а имя лишь подставляется из <code>/etc/passwd</code>. Права на файлы хранятся по UID. Поэтому при монтировании диска или тома в контейнер файлы «меняют владельца»: число то же, а какому имени оно соответствует — зависит от системы.</p><p><b>Флаг <code>-a</code> обязателен.</b> <code>usermod -G sudo deploy</code> без него <i>заменяет</i> весь список групп пользователя на указанный. Так люди отбирают у себя доступ и остаются заперты снаружи сервера.</p>"},
 {h:"sudo и точечные права",b:"<pre>sudo systemctl restart nginx   <span class='c'># одна команда от root</span>\nsudo -u app whoami             <span class='c'># от имени конкретного пользователя</span>\nsudo -i                        <span class='c'># интерактивная сессия root</span>\nvisudo                         <span class='c'># безопасно править /etc/sudoers</span></pre><p>В <code>/etc/sudoers.d/</code> можно выдавать права точечно:</p><pre># /etc/sudoers.d/deploy\ndeploy ALL=(ALL) NOPASSWD: /bin/systemctl restart api, /bin/systemctl status api</pre><p>Это позволяет скрипту деплоя перезапускать один сервис, не получая root на всё остальное.</p><p><b>Правь sudoers только через <code>visudo</code>.</b> Он проверяет синтаксис перед сохранением: ошибка в этом файле блокирует sudo для всех, и восстанавливаться придётся через консоль провайдера или режим восстановления.</p><p><b>Осторожно с шаблонами:</b> право на <code>/bin/systemctl *</code> или на любой редактор фактически равно полному root, потому что через них можно выполнить что угодно.</p>"},
 {h:"Сервисные пользователи",b:"<p>Приложение не должно работать от root. Для него создают отдельного пользователя без домашнего каталога и без интерактивной оболочки:</p><pre>useradd -r -s /usr/sbin/nologin -M app</pre><p><code>-r</code> — системный пользователь (UID из низкого диапазона), <code>-s /usr/sbin/nologin</code> — вход невозможен, <code>-M</code> — без домашнего каталога.</p><p>Если приложение скомпрометируют, атакующий не получит рабочую оболочку и не сможет закрепиться обычными способами.</p><p><b>Одна идея на трёх уровнях:</b> <code>User=app</code> в systemd-юните, <code>USER node</code> в Dockerfile, <code>runAsNonRoot: true</code> в Kubernetes. Это один и тот же принцип наименьших привилегий, выраженный разными синтаксисами.</p>"},
 {h:"Опасные группы",b:"<p>Некоторые группы фактически дают права root, хотя выглядят безобидно:</p><ul><li><b>docker</b> — можно запустить контейнер, примонтировав корень хоста, и получить полный доступ. Это не уязвимость, а свойство архитектуры демона.</li><li><b>disk</b> — прямой доступ к блочным устройствам в обход прав файловой системы.</li><li><b>sudo</b> / <b>wheel</b> — очевидно.</li></ul><p>Выдавать членство в них нужно так же осознанно, как sudo. Формулировка «просто добавь меня в docker, чтобы не писать sudo» на практике означает «дай мне root».</p>"},
 {h:"Обновления и безопасность",b:"<pre>apt list --upgradable\napt-get -s upgrade            <span class='c'># сухой прогон: что будет обновлено</span>\nunattended-upgrades           <span class='c'># автоматические обновления безопасности</span>\nneeds-restart -r              <span class='c'># нужен ли перезапуск сервисов после обновления</span></pre><p>Позиция «работает — не трогай» в отношении обновлений означает накопление известных уязвимостей: большинство взломов серверов использует давно исправленные дыры.</p><p>Разумная практика: автоматические обновления безопасности на всех машинах плюс контролируемое обновление остального с проверкой на staging. И отдельно помнить, что обновление библиотеки не действует на уже запущенные процессы — им нужен перезапуск.</p>"}],
 ch:[
 {t:"c",d:1,q:"Установи nginx на Ubuntu без интерактивных подтверждений.",ans:["apt install -y nginx","sudo apt install -y nginx","apt-get install -y nginx","sudo apt-get install -y nginx"],hint:"apt install и флаг «отвечать да»",why:"Перед установкой нужен apt update, иначе используется устаревший индекс: пакет может не найтись или установится версия с уже исправленной уязвимостью."},
 {t:"q",d:2,q:"Чем <code>apt remove</code> отличается от <code>apt purge</code> и когда разница становится важной?",a:["Ничем, это синонимы","purge удаляет ещё и конфигурационные файлы из /etc; разница важна при переустановке «начисто», когда старый конфиг мешает","remove работает быстрее","purge применим только к ядру"],c:1,why:"После remove конфиги остаются: иногда это спасает при случайном удалении пакета, иногда мешает — новая установка подхватывает старые настройки, и человек долго не понимает, откуда они берутся."},
 {t:"c",d:2,q:"Пользователю deploy нужен доступ к docker. Добавь его в группу docker, сохранив все остальные группы.",ans:["usermod -aG docker deploy","sudo usermod -aG docker deploy"],hint:"два флага: append и group",why:"Без флага -a список групп будет полностью перезаписан. И отдельно помни: членство в группе docker фактически равно правам root на хосте."},
 {t:"q",d:3,q:"Зачем сервисному пользователю задают оболочку <code>/usr/sbin/nologin</code> и как этот же принцип выглядит в контейнерах?",a:["Чтобы сэкономить место","Чтобы под этим аккаунтом нельзя было войти интерактивно и закрепиться после компрометации; в Dockerfile тот же принцип — инструкция USER, в Kubernetes — runAsNonRoot","Так требует systemd","Чтобы ускорить запуск сервиса"],c:1,why:"Это один принцип наименьших привилегий на трёх уровнях. Скомпрометировав приложение, атакующий не получает рабочий shell и лишается самых удобных путей закрепления."},
 {t:"q",d:4,q:"Скрипту деплоя нужно перезапускать один сервис. Какой вариант выдачи прав правильный и какой распространённый вариант на самом деле равен полному root?",a:["Полный sudo без пароля — просто и работает","Точечное правило в /etc/sudoers.d на конкретную команду; при этом право на <code>systemctl *</code> или на любой редактор фактически равно root, потому что через них выполняется произвольный код","Запускать скрипт от root напрямую","Поставить бинарнику systemctl бит SUID"],c:1,why:"Наименьшие привилегии означают конкретную команду, а не шаблон. Правило со звёздочкой или с текстовым редактором создаёт иллюзию ограничения, не ограничивая ничего."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Коллега выполнил на боевом сервере <code>usermod -G docker deploy</code> и вышел из сессии. Теперь deploy не может выполнить ни одной команды через sudo, других учётных записей с доступом нет. Что произошло, как восстановиться и какое правило это предотвращает?",a:["Сломался sudo, нужна переустановка пакета","Без флага -a список групп заменился, и deploy выпал из группы sudo. Восстановление — через веб-консоль провайдера или режим восстановления; правило: при любых изменениях доступа держать вторую активную сессию открытой, пока новая конфигурация не проверена","Истёк срок действия пароля пользователя","Группа docker конфликтует с группой sudo"],c:1,why:"Это правило универсально для sudo, sshd и фаервола: пока новый способ входа не проверен отдельной сессией, старую закрывать нельзя. Оно стоит ноль и спасает от поездки в консоль провайдера."}]},

{id:"l9",t:"Vim: выжить и отредактировать",goal:"Править конфиг на сервере, где нет твоего редактора",
 cards:[
 {h:"Почему это обязательный навык",b:"<p><code>vi</code> входит в базовую поставку практически любой Unix-системы. Nano есть не везде, вашего любимого редактора нет нигде, а в минимальном контейнере может не быть вообще ничего, кроме <code>vi</code>.</p><p>Рано или поздно ты окажешься в ситуации: продакшен лежит, нужно поправить одну строку в конфиге, и на экране непонятный редактор, который не реагирует на набор текста. Базовый набор из десяти команд закрывает эту ситуацию полностью.</p>"},
 {h:"Два режима — вся суть",b:"<p>Vim открывается в <b>командном</b> режиме: буквы на клавиатуре — это команды, а не текст. Чтобы печатать, нужно перейти в режим вставки.</p><pre>i     <span class='c'># вставка перед курсором</span>\na     <span class='c'># вставка после курсора</span>\nI     <span class='c'># вставка в начало строки</span>\nA     <span class='c'># вставка в конец строки</span>\no     <span class='c'># новая строка ниже и вставка</span>\nEsc   <span class='c'># вернуться в командный режим</span></pre><p><b>Правило выживания:</b> если непонятно, что происходит, нажми <code>Esc</code>. Это возвращает в известное состояние из любого режима.</p><div class='analogy'><b>Аналогия:</b> боевой режим и режим строительства. Нажал не ту кнопку не в том режиме — построил дом вместо выстрела.</div>"},
 {h:"Выйти, сохранить, не потерять",b:"<pre>:w     <span class='c'># сохранить</span>\n:q     <span class='c'># выйти</span>\n:wq    <span class='c'># сохранить и выйти</span>\n:x     <span class='c'># то же, но не трогает файл без изменений</span>\n:q!    <span class='c'># выйти, отбросив изменения</span></pre><p>Все команды с двоеточием набираются в командном режиме — если ничего не происходит, сначала <code>Esc</code>.</p><p><b>Приём, который спасал тысячи людей.</b> Открыл системный конфиг без sudo, всё отредактировал, а записать не можешь:</p><pre>:w !sudo tee %</pre><p><code>%</code> означает текущий файл. Команда передаёт содержимое буфера в <code>tee</code>, запущенный от root. На будущее лучше сразу использовать <code>sudoedit файл</code>: он открывает временную копию и подставляет её обратно с нужными правами.</p>"},
 {h:"Минимум для реальной работы",b:"<pre>/слово    <span class='c'># поиск вперёд; n следующее, N предыдущее</span>\n?слово    <span class='c'># поиск назад</span>\ndd        <span class='c'># удалить строку (она попадает в буфер)</span>\n5dd       <span class='c'># удалить пять строк</span>\nyy  p     <span class='c'># копировать строку / вставить ниже</span>\nu         <span class='c'># отменить; Ctrl+r — вернуть отменённое</span>\ngg  G     <span class='c'># в начало / в конец файла</span>\n:15       <span class='c'># перейти к строке 15</span>\n:set nu   <span class='c'># показать номера строк</span>\nx         <span class='c'># удалить символ под курсором</span></pre><p>Номера строк критичны при отладке конфигов: <code>nginx -t</code> сообщает «ошибка в строке 42», и с включённой нумерацией ты попадаешь туда мгновенно вместо пролистывания.</p>"},
 {h:"Замена по файлу",b:"<pre>:%s/8080/3000/g     <span class='c'># все вхождения во всём файле</span>\n:%s/8080/3000/gc    <span class='c'># с подтверждением каждой замены</span>\n:5,20s/old/new/g    <span class='c'># только в строках 5-20</span>\n:%s/\\s\\+$//         <span class='c'># убрать пробелы в конце строк</span></pre><p>Расшифровка: <code>%</code> — весь файл, <code>s</code> — замена, <code>g</code> — все вхождения в строке (без него только первое), <code>c</code> — спрашивать подтверждение.</p><p>Синтаксис совпадает с <code>sed</code> — это родственники. Выучив одно, ты почти бесплатно получаешь второе.</p><p>Флаг <code>c</code> стоит использовать на проде: он превращает массовую замену из «надеюсь, я не заменил лишнего» в осознанную операцию.</p>"},
 {h:"Восстановление после сбоя",b:"<p>Если сессия оборвалась (упал SSH, закрылся терминал), vim оставляет файл подкачки <code>.имя.swp</code>. При следующем открытии он предложит восстановить несохранённые изменения.</p><p>Оттуда же берётся сообщение «swap file already exists» при открытии файла, который редактируют в другой сессии. Это защита от двух одновременных правок одного конфига — не игнорируй её бездумно: возможно, коллега прямо сейчас правит тот же файл.</p><pre>vim -r файл    <span class='c'># восстановить из swap</span></pre>"},
 {h:"Nano и когда чем пользоваться",b:"<p><code>nano</code> проще: печатаешь сразу, подсказки внизу экрана, <code>Ctrl+O</code> сохранить, <code>Ctrl+X</code> выйти, <code>Ctrl+W</code> поиск.</p><p>Практический вывод: для повседневной правки бери то, что удобнее. Но базовый набор vim знать обязательно — иначе однажды окажешься в контейнере или на голом сервере наедине с непонятным экраном, когда прод лежит и время идёт.</p><p>И ещё одна причина: vim-режим встроен в <code>less</code>, <code>man</code>, <code>git</code> и множество других инструментов. Клавиши <code>/</code>, <code>n</code>, <code>q</code>, <code>gg</code>, <code>G</code> работают везде.</p>"}],
 ch:[
 {t:"q",d:1,q:"Ты открыл vim, начинаешь печатать — текст не появляется, экран издаёт звуки. Что происходит и что нажать?",a:["Клавиатура не работает в SSH-сессии","Ты в командном режиме, где буквы — это команды; нажми i для перехода в режим вставки","Файл открыт только для чтения","Vim завис и требует перезапуска"],c:1,why:"Первое потрясение каждого новичка. Esc всегда возвращает в командный режим — это точка, из которой можно начать заново, если непонятно, что происходит."},
 {t:"c",d:2,q:"Ты внёс правки в конфиг и хочешь сохранить файл и выйти из vim одной командой.",ans:[":wq",":x","zz",":wq!"],hint:"начинается с двоеточия",why:":wq записывает и выходит, :q! выходит без сохранения. Обе нужно помнить: первая завершает работу, вторая спасает, когда наделал лишнего."},
 {t:"c",d:3,q:"В конфиге приложения нужно заменить все упоминания порта 8080 на 3000 по всему файлу. Напиши команду vim.",ans:[":%s/8080/3000/g"],hint:"% — весь файл, g — все вхождения в строке",why:"Без флага g заменится только первое вхождение в каждой строке — легко пропустить второе упоминание порта в той же строке. Добавив c, можно подтверждать каждую замену вручную."},
 {t:"q",d:3,q:"<code>nginx -t</code> сообщает об ошибке в строке 42 конфигурационного файла. Как быстрее всего попасть в это место в vim?",a:["Листать стрелками, считая строки","Включить нумерацию <code>:set nu</code> и перейти командой <code>:42</code>","Найти поиском по тексту ошибки","Открыть файл заново с флагом -n"],c:1,why:"Нумерация плюс переход по номеру превращают отладку конфигов из пролистывания в одно действие. Ещё быстрее — открыть сразу нужную строку: vim +42 файл."},
 {t:"q",d:4,q:"Ты открыл <code>/etc/nginx/nginx.conf</code> без sudo, внёс двадцать правок, и команда <code>:w</code> отвечает «Read-only file». Как сохранить работу и как избежать этого в следующий раз?",a:["Выйти и переписать всё заново под sudo","Выполнить <code>:w !sudo tee %</code> — содержимое буфера записывается через sudo; на будущее открывать системные файлы командой sudoedit","Изменить права на файл через chmod 666","Скопировать текст мышью в другой файл"],c:1,why:"% в командной строке vim означает текущий файл. Приём известен каждому, кто хоть раз правил конфиг на проде. sudoedit решает задачу заранее и безопаснее: он работает с временной копией."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Ты правил конфигурацию nginx на проде в vim, случайно нажал несколько клавиш в командном режиме и не понимаешь, что изменилось в файле. Изменения ещё не сохранены. Какое действие самое безопасное и почему?",a:["Сохранить и посмотреть, что получилось, потом исправить","Выйти без сохранения через <code>:q!</code> и начать заново с целого файла — пока не было :w, файл на диске не тронут","Нажать u несколько раз и сохранить","Перезапустить nginx, чтобы проверить конфиг"],c:1,why:"Пока команда записи не выполнена, оригинал на диске цел — выход без сохранения бесплатно возвращает известное состояние. Сохранять «чтобы посмотреть» на проде нельзя: nginx -t проверит синтаксис, но не заметит осмысленную, но неверную правку."}]},

{id:"l10",t:"Текстовые потоки: sed, awk, пайпы",goal:"Обрабатывать логи и вывод, не открывая редактор",
 cards:[
 {h:"Три потока",b:"<p>У каждого процесса три стандартных потока, у каждого свой номер:</p><div class='tw'><table><tr><th>номер</th><th>поток</th><th>назначение</th></tr><tr><td>0</td><td>stdin</td><td>вход</td></tr><tr><td>1</td><td>stdout</td><td>полезный вывод</td></tr><tr><td>2</td><td>stderr</td><td>ошибки и диагностика</td></tr></table></div><p>Разделение вывода и ошибок — не формальность: оно позволяет передать полезные данные дальше по конвейеру, а сообщения об ошибках оставить человеку. Именно поэтому <code>grep</code> в пайпе не засоряется предупреждениями предыдущей команды.</p>"},
 {h:"Перенаправление и порядок операций",b:"<pre>cmd &gt; out.log         <span class='c'># stdout в файл, перезапись</span>\ncmd &gt;&gt; out.log        <span class='c'># дописать в конец</span>\ncmd 2&gt; err.log        <span class='c'># только ошибки</span>\ncmd &gt; all.log 2&gt;&amp;1    <span class='c'># оба потока в один файл</span>\ncmd &amp;&gt; all.log        <span class='c'># то же, короткая форма bash</span>\ncmd 2&gt;/dev/null       <span class='c'># выбросить ошибки</span>\ncmd &lt; input.txt       <span class='c'># подать файл на вход</span></pre><p><b>Порядок критичен и неинтуитивен.</b> <code>&gt; file 2&gt;&amp;1</code> работает так: сначала stdout направляется в файл, потом stderr привязывается «туда же, куда сейчас указывает stdout» — то есть в файл.</p><p>А <code>2&gt;&amp;1 &gt; file</code> делает обратное: stderr привязывается к терминалу (куда пока указывает stdout), и только затем stdout уходит в файл. Ошибки останутся на экране.</p><p>Это одна из тех деталей, которые невозможно вывести логически — их просто запоминают. И именно из-за неё половина скриптов теряет сообщения об ошибках.</p>"},
 {h:"sed — потоковый редактор",b:"<pre>sed 's/old/new/' f          <span class='c'># первое вхождение в каждой строке</span>\nsed 's/old/new/g' f         <span class='c'># все вхождения</span>\nsed -n '10,20p' f           <span class='c'># напечатать только строки 10-20</span>\nsed -n '/09:40/,/09:50/p' f <span class='c'># диапазон между совпадениями</span>\nsed '/^#/d' f               <span class='c'># удалить строки-комментарии</span>\nsed 's/;.*$//' f            <span class='c'># обрезать всё после точки с запятой</span>\nsed -i.bak 's/8080/3000/g' app.conf</pre><p><code>-n</code> отключает автоматический вывод строк, поэтому в паре с <code>p</code> печатается только то, что выбрано.</p><p><code>-i</code> изменяет файл на месте, <code>-i.bak</code> дополнительно сохраняет оригинал как <code>app.conf.bak</code>.</p><p><b>Порядок работы на проде:</b> сначала прогнать без <code>-i</code> и посмотреть вывод, убедиться, что заменяется ровно то, что нужно, и только потом добавлять флаг. Массовая замена по всему файлу без предварительной проверки — способ узнать, что такое паника.</p>"},
 {h:"awk — работа по колонкам",b:"<pre>awk '{print $1}' access.log            <span class='c'># первая колонка</span>\nawk '{print $1, $9}' access.log       <span class='c'># несколько колонок</span>\nawk '$9 == 500 {print $7}' access.log <span class='c'># URL, где код ответа 500</span>\nawk '$10 &gt; 1000 {print}' access.log   <span class='c'># строки, где значение больше</span>\nawk '{sum+=$10} END {print sum}' f    <span class='c'># сумма колонки</span>\nawk -F: '{print $1}' /etc/passwd      <span class='c'># свой разделитель</span></pre><p><code>$1</code>, <code>$2</code> — колонки, разделённые пробелами; <code>$0</code> — вся строка; <code>NF</code> — число полей; <code>NR</code> — номер строки.</p><p>Условие перед блоком фильтрует строки — это делает awk мини-языком, а не просто «резалкой колонок». Блок <code>END</code> выполняется после обработки всех строк, поэтому в нём удобно печатать итоги.</p>"},
 {h:"Классический однострочник",b:"<pre>awk '{print $1}' access.log | sort | uniq -c | sort -rn | head</pre><p>Разбор по шагам:</p><ol><li><code>awk</code> достаёт колонку с IP-адресом</li><li><code>sort</code> ставит одинаковые строки рядом</li><li><code>uniq -c</code> схлопывает соседние повторы и считает их</li><li><code>sort -rn</code> сортирует по числу, по убыванию</li><li><code>head</code> оставляет первую десятку</li></ol><p><b>Почему sort обязателен.</b> <code>uniq</code> схлопывает только <i>соседние</i> одинаковые строки — так он работает без загрузки всего файла в память. Без предварительной сортировки счётчики будут неверными, и, что хуже, команда отработает без ошибки: ты получишь правдоподобные, но неправильные числа.</p><p>Это самая частая логическая ошибка в однострочниках, и она не проявляется никак, кроме неверного ответа.</p>"},
 {h:"Остальной инструментарий",b:"<pre>sort -h        <span class='c'># сортировка человеческих размеров: 5K, 2M, 1G</span>\nsort -rn       <span class='c'># по числу, по убыванию</span>\nsort -k2       <span class='c'># по второй колонке</span>\ncut -d: -f1 /etc/passwd   <span class='c'># колонка по разделителю</span>\ntr -d ' '      <span class='c'># удалить символы</span>\ntr 'A-Z' 'a-z' <span class='c'># привести к нижнему регистру</span>\ndiff a.conf b.conf        <span class='c'># чем отличаются два файла</span>\ncomm -13 a.txt b.txt      <span class='c'># строки, которые есть только во втором</span>\nxargs          <span class='c'># превратить список в аргументы команды</span></pre><p><code>diff</code> незаменим при разборе «на стейдже работает, на проде нет»: сравнил два конфига — увидел причину за секунду вместо часа чтения.</p><p><code>xargs</code> связывает поиск и действие: <code>find /tmp -name '*.tmp' | xargs rm</code>. Для имён с пробелами нужна пара <code>find -print0</code> и <code>xargs -0</code>.</p>"},
 {h:"Как это спрашивают на собеседовании",b:"<p>Типичная задача: «в access.log найди десять IP с наибольшим числом запросов» или «посчитай распределение кодов ответа».</p><pre>awk '{print $9}' access.log | sort | uniq -c | sort -rn</pre><p>Проверяют не память на флаги, а умение <b>собирать конвейер</b>: разложить задачу на простые шаги и соединить их. Если по дороге ты сам скажешь, почему перед uniq нужен sort, — это заметный плюс.</p><p>Второй частый вопрос — про перенаправление: «как записать и вывод, и ошибки в один файл». Правильный ответ с объяснением порядка выделяет кандидата.</p>"}],
 ch:[
 {t:"c",d:1,q:"Запиши обычный вывод скрипта ./deploy.sh в файл deploy.log, перезаписав его прежнее содержимое.",ans:["./deploy.sh > deploy.log","./deploy.sh >deploy.log"],hint:"один символ перенаправления",why:"Один знак > перезаписывает, два >> дописывают. Перепутал — потерял историю прошлых запусков, которая могла понадобиться при разборе."},
 {t:"c",d:2,q:"Тот же скрипт нужно запустить так, чтобы и обычный вывод, и сообщения об ошибках попали в один файл deploy.log.",ans:["./deploy.sh > deploy.log 2>&1","./deploy.sh >deploy.log 2>&1","./deploy.sh &> deploy.log"],hint:"сначала перенаправить stdout, затем привязать stderr туда же",why:"Порядок принципиален: 2>&1 копирует текущее назначение stdout. Написав 2>&1 перед > file, ты привяжешь ошибки к терминалу, и они не попадут в файл."},
 {t:"c",d:3,q:"В конфигурации app.conf нужно заменить все вхождения порта 8080 на 3000 прямо в файле, сохранив резервную копию оригинала.",ans:["sed -i.bak s/8080/3000/g app.conf","sed -i.bak 's/8080/3000/g' app.conf"],hint:"sed -i с суффиксом бэкапа и флагом g",why:"-i.bak создаёт app.conf.bak с исходным содержимым. Бэкап ничего не стоит и однажды спасёт: массовая замена по всему файлу почти всегда задевает больше, чем ожидалось."},
 {t:"q",d:4,q:"Разбери конвейер <code>awk '{print $1}' access.log | sort | uniq -c | sort -rn | head</code>: что он делает и зачем в нём первый <code>sort</code>?",a:["Сортирует лог по алфавиту и показывает начало","Показывает десять IP с наибольшим числом запросов; первый sort обязателен, потому что uniq схлопывает только соседние одинаковые строки — без него счётчики будут неверными","Удаляет дубликаты строк из лога","Считает общее число запросов"],c:1,why:"Коварство ошибки в том, что без sort команда отработает успешно и выдаст правдоподобные числа. Неверный результат не отличить от верного, если не знать механики uniq."},
 {t:"q",d:4,q:"Нужно посчитать распределение кодов ответа в access.log, где код находится в девятой колонке. Какой конвейер решает задачу?",a:["<code>grep -c 200 access.log</code>","<code>awk '{print $9}' access.log | sort | uniq -c | sort -rn</code>","<code>cat access.log | wc -l</code>","<code>sed -n '9p' access.log</code>"],c:1,why:"Тот же шаблон «взять колонку → отсортировать → посчитать повторы → отсортировать по числу». Освоив его, ты закрываешь большинство задач анализа логов одной строкой."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Сайт лёг под нагрузкой, времени мало. Нужно за минуту понять, это реальный наплыв пользователей или один источник забивает канал. Какая последовательность даст ответ быстрее всего и почему именно она?",a:["Открыть access.log в редакторе и просмотреть последние записи","<code>tail -50000 access.log | awk '{print $1}' | sort | uniq -c | sort -rn | head</code> — сначала сузить окно свежим хвостом, затем посчитать частоты по адресам","Посчитать общее число строк через wc -l и сравнить со вчерашним","Перезапустить nginx и посмотреть, повторится ли"],c:1,why:"Сначала окно (иначе читается весь файл), потом частоты. Если один-два адреса дают львиную долю запросов — это не наплыв клиентов, и лечится ограничением скорости в nginx или на фаерволе, а не масштабированием."}]}
);
