/* ===== дополнительные миссии (второй блок контента) ===== */
const W=id=>WORLDS.find(w=>w.id===id);
W("linux").miss.push(
{id:"l5",t:"systemd: сервисы навсегда",goal:"Сделать так, чтобы приложение поднималось само",
 cards:[
 {h:"Юнит-файл",b:"<pre>[Unit]\nDescription=Shop API\nAfter=network.target postgresql.service\nRequires=postgresql.service\n\n[Service]\nUser=app\nWorkingDirectory=/opt/app\nExecStart=/opt/app/bin/api --port 8080\nRestart=on-failure\nRestartSec=5\nEnvironmentFile=/etc/app/env\n\n[Install]\nWantedBy=multi-user.target</pre><p>После правки — <code>systemctl daemon-reload</code>, иначе systemd не увидит изменений.</p>"},
 {h:"start vs enable",b:"<p><code>start</code> — запустить сейчас. <code>enable</code> — запускать при загрузке. Это <b>разные вещи</b>, и забытый <code>enable</code> — классическая причина «после ребута всё исчезло».</p><pre>systemctl enable --now api   <span class='c'># и включить, и запустить</span>\nsystemctl is-enabled api\njournalctl -u api -f --since \"10 min ago\"</pre>"}],
 ch:[
 {t:"q",q:"Сервер перезагрузили — приложение не поднялось, хотя вчера работало. Что забыли?",a:["systemctl start","systemctl enable","daemon-reload","chmod"],c:1,why:"enable создаёт симлинк в multi-user.target.wants. Без него сервис не стартует при загрузке."},
 {t:"c",q:"Перечитай изменённые юнит-файлы systemd.",ans:["systemctl daemon-reload","sudo systemctl daemon-reload"],hint:"daemon-...",why:"systemd кэширует юниты в памяти. Без daemon-reload правки просто не применятся, и ты будешь искать несуществующий баг."},
 {t:"q",q:"Что делает <code>Restart=on-failure</code>?",a:["Перезапускает всегда","Перезапускает только при ненулевом коде выхода","Запрещает перезапуск","Перезапускает раз в сутки"],c:1,why:"always перезапускает даже после штатной остановки, on-failure — только после падения. Плюс RestartSec, чтобы не устроить шторм перезапусков."},
 {t:"q",q:"Где смотреть логи сервиса, который пишет в stdout?",a:["/var/log/syslog вручную","journalctl -u имя","cat /dev/stdout","нигде"],c:1,why:"systemd собирает stdout/stderr в журнал. journalctl -u api -f — живой просмотр, --since сужает окно."},
 {t:"f",q:"Допиши директиву, которая гарантирует запуск API только после базы.",pre:"[Unit]\nDescription=Shop API\n___=postgresql.service",ans:["requires","after"],hint:"жёсткая зависимость",why:"Requires= — жёсткая зависимость, After= — только порядок запуска. На практике нужны обе строки."}]},
{id:"l6",t:"Планировщик и фоновые задачи",goal:"Автоматизировать рутину по расписанию",
 cards:[
 {h:"cron",b:"<pre>crontab -e     <span class='c'># редактировать задания пользователя</span>\ncrontab -l\n\n<span class='c'># мин час день месяц день_недели</span>\n0 3 * * *   /opt/scripts/backup.sh\n*/5 * * * * /opt/scripts/check.sh >> /var/log/check.log 2>&amp;1</pre><p>Главная ловушка: у cron <b>минимальное окружение</b> — нет твоего PATH и переменных. Пиши абсолютные пути и подгружай env явно.</p>"},
 {h:"systemd timer — современная замена",b:"<pre># backup.timer\n[Timer]\nOnCalendar=*-*-* 03:00:00\nPersistent=true</pre><p>Плюсы перед cron: логи в journalctl, зависимости, <code>Persistent=true</code> догоняет пропущенный запуск после выключения машины.</p>"}],
 ch:[
 {t:"q",q:"Что означает <code>*/5 * * * *</code>?",a:["Каждые 5 часов","Каждые 5 минут","Раз в 5 дней","В 5:00"],c:1,why:"Первое поле — минуты. */5 = каждые пять минут. Порядок полей: мин, час, день месяца, месяц, день недели."},
 {t:"q",q:"Скрипт работает руками, но молча не работает из cron. Первая гипотеза?",a:["Cron сломан","Другое окружение: PATH и переменные не те, нужны абсолютные пути","Нет прав root","Диск полон"],c:1,why:"И вторая гипотеза — не перенаправлен вывод, поэтому ошибку никто не видит. Всегда добавляй >> log 2>&1."},
 {t:"q",q:"Чем systemd timer лучше cron для продакшена?",a:["Ничем","Логи в journalctl, зависимости от других юнитов и догон пропущенных запусков","Быстрее","Проще синтаксис"],c:1,why:"Плюс timer можно запустить вручную как обычный сервис и посмотреть результат — с cron так не получится."},
 {t:"q",q:"Бэкап по cron «работает» полгода. Что обязательно проверить?",a:["Размер лога","Что бэкап реально восстанавливается — тестовое восстановление","Версию cron","Права на скрипт"],c:1,why:"Непроверенный бэкап — это не бэкап. Восстановление тестируют по расписанию, а не в день аварии."}]}
);
W("net").miss.push(
{id:"n5",t:"Firewall и доступы",goal:"Закрыть всё лишнее и не отрезать себя",
 cards:[
 {h:"Принцип",b:"<p>По умолчанию — <b>запрещено всё входящее</b>, разрешено только нужное. Открытый наружу порт БД — самая частая причина взлома небольших проектов.</p><pre>ufw default deny incoming\nufw allow 22/tcp\nufw allow 80,443/tcp\nufw enable\nufw status numbered</pre>"},
 {h:"Как не отрезать себе руки",b:"<p>Сначала разреши SSH, потом включай фаервол. Если работаешь удалённо — держи вторую открытую сессию, пока не убедишься, что новое правило не выкинуло тебя.</p><p>В облаке фаервол обычно двухслойный: security group у провайдера + локальный. Проверяй оба, когда «порт открыт, но не отвечает».</p>"}],
 ch:[
 {t:"q",q:"Правильная политика по умолчанию для входящего трафика?",a:["allow","deny","reject всё, включая 22","зависит"],c:1,why:"deny incoming + точечные разрешения. Обратный подход («закроем плохое») не масштабируется."},
 {t:"q",q:"Порт 5432 открыт в security group на 0.0.0.0/0. Насколько это плохо?",a:["Нормально, есть пароль","Критично: БД доступна всему интернету, её найдут сканеры за часы","Не важно","Только если нет TLS"],c:1,why:"Публичные БД сканируют массово. Доступ — только из приватной сети или через туннель."},
 {t:"c",q:"Проверь, слушает ли что-то порт 5432 и какой процесс.",ans:["ss -tulpn | grep 5432","ss -tulpn|grep 5432","netstat -tulpn | grep 5432","ss -tulpn | grep postgres"],hint:"ss + пайп в grep",why:"Смотри ещё и адрес: 127.0.0.1:5432 — только локально (хорошо), 0.0.0.0:5432 — на всех интерфейсах (опасно)."},
 {t:"q",q:"Порт открыт в ufw, приложение слушает, но снаружи не подключиться. Что проверить в облаке?",a:["Только DNS","Security group / сетевой ACL провайдера — второй слой фильтрации","Версию ядра","Права на файл"],c:1,why:"Два фаервола — две точки отказа. Проверяй оба слоя и маршрут (traceroute/nc)."}]},
{id:"n6",t:"Балансировка и заголовки",goal:"Понять, что видит приложение за прокси",
 cards:[
 {h:"X-Forwarded-For и Host",b:"<p>За прокси приложение видит IP <b>балансировщика</b>, а не клиента. Реальный адрес приходит в заголовках:</p><pre>proxy_set_header Host              $host;\nproxy_set_header X-Real-IP         $remote_addr;\nproxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;\nproxy_set_header X-Forwarded-Proto $scheme;</pre><p>Забыл <code>Host</code> — приложение строит неправильные ссылки и редиректы. Забыл <code>X-Forwarded-Proto</code> — получишь бесконечный редирект http↔https.</p>"},
 {h:"Алгоритмы и здоровье",b:"<ul><li><b>round-robin</b> — по кругу (по умолчанию).</li><li><b>least_conn</b> — тому, у кого меньше активных соединений.</li><li><b>ip_hash</b> / sticky — привязка клиента к бэкенду (нужно для сессий в памяти).</li></ul><p>Балансировщик должен проверять здоровье бэкендов и выводить больные из ротации, иначе он честно раздаёт трафик в мёртвый узел.</p>"}],
 ch:[
 {t:"q",q:"В логах приложения у всех запросов один и тот же IP. Почему?",a:["Атака с одного адреса","Это IP балансировщика; реальный клиент — в X-Forwarded-For","Ошибка приложения","Проблема DNS"],c:1,why:"Нужно и передать заголовок в прокси, и научить приложение ему доверять (trusted proxies) — иначе подделают."},
 {t:"q",q:"Бесконечный редирект после включения HTTPS на балансировщике. Причина?",a:["Плохой сертификат","Приложение не знает, что снаружи https — не передан X-Forwarded-Proto","DNS","Кэш браузера"],c:1,why:"Приложение видит http и редиректит на https, балансировщик снова отдаёт http внутрь. Петля."},
 {t:"q",q:"Сессии пользователей рвутся при балансировке на 3 сервера. Быстрое и правильное решения?",a:["Только ip_hash навсегда","Быстро — sticky-сессии; правильно — хранить сессии в Redis/БД, сделав приложение stateless","Один сервер","Отключить балансировку"],c:1,why:"Stateless-приложение масштабируется горизонтально без ограничений. Sticky — костыль на переходный период."},
 {t:"q",q:"Зачем балансировщику healthcheck бэкендов?",a:["Для статистики","Чтобы не отправлять трафик в неработающий узел","Для логов","Для сертификатов"],c:1,why:"Без проверки здоровья часть пользователей стабильно получает ошибки — ровно 1/N трафика."}]}
);
W("git").miss.push(
{id:"g4",t:"Версии, теги и релизы",goal:"Связать код с тем, что реально работает на проде",
 cards:[
 {h:"Semver и теги",b:"<pre>v2.4.1\n│ │ └── patch: исправление, обратно совместимо\n│ └──── minor: новая функция, совместимо\n└────── major: ломающее изменение</pre><pre>git tag -a v2.4.1 -m \"релиз\"\ngit push origin v2.4.1</pre><p>Аннотированный тег (<code>-a</code>) хранит автора и дату — по нему пайплайн запускает прод-деплой.</p>"},
 {h:"Связь тега и артефакта",b:"<p>Образ тегируется хешем коммита: <code>registry/app:1a2b3c4</code>. Тогда по работающему контейнеру ты за секунду находишь точный код. <code>latest</code> такой связи не даёт — и расследование инцидента превращается в гадание.</p>"}],
 ch:[
 {t:"q",q:"Изменили формат ответа API, старые клиенты сломаются. Какую часть версии поднимаем?",a:["patch","minor","major","никакую"],c:2,why:"Ломающее изменение = major. Это контракт с потребителями твоего API."},
 {t:"c",q:"Создай аннотированный тег v1.0.0 с сообщением «первый релиз».",ans:['git tag -a v1.0.0 -m первый релиз','git tag -a v1.0.0 -m "первый релиз"',"git tag -a v1.0.0"],hint:"git tag -a ... -m ...",why:"Затем git push origin v1.0.0 — теги не уезжают обычным git push."},
 {t:"q",q:"Как быстро понять, какой код работает в контейнере на проде?",a:["Спросить разработчика","По тегу образа = хеш коммита","Открыть логи","Никак"],c:1,why:"Неизменяемые теги — основа воспроизводимости. Ещё лучше — вшить версию и git-sha в /health-эндпоинт."},
 {t:"q",q:"Что такое CHANGELOG и откуда он берётся у зрелой команды?",a:["Пишется вручную перед релизом","Генерируется автоматически из conventional commits","Не нужен","Из логов CI"],c:1,why:"feat/fix/BREAKING CHANGE → автоматический changelog и версия. Дисциплина коммитов окупается."}]}
);
/* миссии Docker и CI/CD перенесены в p21/p22 */
W("iac").miss.push(
{id:"i4",t:"Модули и окружения",goal:"Не копировать конфиги между prod и staging",
 cards:[
 {h:"Модуль",b:"<pre>module \"app\" {\n  source        = \"./modules/service\"\n  env           = \"prod\"\n  instance_type = \"t3.large\"\n  replicas      = 3\n}</pre><p>Внутри модуля — ресурсы и переменные. Снаружи — только параметры. Это обычная функция, но для инфраструктуры.</p>"},
 {h:"Разделение окружений",b:"<pre>infra/\n  modules/service/\n  envs/\n    staging/main.tf   -> module \"app\" { replicas = 1 }\n    prod/main.tf      -> module \"app\" { replicas = 3 }</pre><p>У каждого окружения <b>свой state</b>. Ошибка в staging физически не может задеть prod. Альтернатива — workspaces, но отдельные каталоги нагляднее и безопаснее.</p>"}],
 ch:[
 {t:"q",q:"Главная причина разделять state по окружениям?",a:["Скорость","Изоляция: apply в staging не может задеть prod","Экономия","Требование Terraform"],c:1,why:"Общий state — один неверный apply от катастрофы. Плюс разные права доступа к разным state."},
 {t:"q",q:"Что выносят в модуль?",a:["Всё подряд","Повторяющийся набор ресурсов с параметрами","Только переменные","Секреты"],c:1,why:"Модуль оправдан, когда набор используется 2+ раз. Преждевременная модуляризация усложняет чтение."},
 {t:"q",q:"Как передать значение из одного модуля в другой?",a:["Через файл","Через output одного и input другого","Через глобальные переменные","Никак"],c:1,why:"output → module.app.ip → вход другого модуля. Terraform сам строит граф зависимостей по этим ссылкам."},
 {t:"q",q:"Зачем в Terraform фиксировать версии провайдеров?",a:["Не нужно","Иначе завтрашний apply может повести себя иначе и сломать прод","Для скорости","Для лицензии"],c:1,why:"required_providers с оператором ~> и .terraform.lock.hcl в git — воспроизводимость сборки инфраструктуры."}]}
);
W("k8s").miss.push(
{id:"k5",t:"Helm и GitOps",goal:"Управлять релизами, а не отдельными YAML",
 cards:[
 {h:"Helm",b:"<pre>helm install api ./chart -f values-prod.yaml\nhelm upgrade api ./chart --set image.tag=1a2b3c4\nhelm rollback api 3\nhelm list</pre><p>Chart — шаблоны манифестов, values — параметры окружения. Один chart, разные values для stage и prod.</p>"},
 {h:"GitOps",b:"<p>Желаемое состояние живёт в git. Агент (ArgoCD, Flux) непрерывно сравнивает кластер с репозиторием и устраняет расхождение (drift).</p><ul><li>Выкатка = merge в git. Откат = <code>git revert</code>.</li><li>Полный аудит: кто, когда, что изменил.</li><li>Ручной <code>kubectl apply</code> на проде становится аномалией, которую агент откатит.</li></ul>"}],
 ch:[
 {t:"q",q:"Ты вручную сделал kubectl scale на кластере под ArgoCD. Что произойдёт?",a:["Останется навсегда","Агент вернёт состояние из git (drift будет устранён)","Кластер упадёт","Ничего"],c:1,why:"Поэтому в GitOps любое постоянное изменение фиксируется в репозитории, а ручные команды — только для аварийной стабилизации."},
 {t:"q",q:"Зачем нужен Helm, если есть kubectl apply?",a:["Он быстрее","Шаблоны + параметры окружений + версионирование релизов и откат одной командой","Он безопаснее","Обязателен в k8s"],c:1,why:"Альтернативы: Kustomize (наложение патчей без шаблонов), jsonnet. Helm — самый распространённый."},
 {t:"q",q:"Как в GitOps откатить неудачный релиз?",a:["kubectl delete","git revert коммита — агент приведёт кластер обратно","Перезагрузить ноды","Никак"],c:1,why:"История релизов = история git. Это и есть главная ценность подхода."},
 {t:"q",q:"Где в GitOps хранить секреты?",a:["Прямо в git","Sealed Secrets / External Secrets / Vault — в git попадает только зашифрованное или ссылка","В values.yaml","В образе"],c:1,why:"Иначе весь смысл «всё в git» упирается в утечку. SOPS+age или External Secrets Operator — стандартные решения."}]},
{id:"k6",t:"Состояние и хранилище",goal:"Запускать БД и очереди в кластере осознанно",
 cards:[
 {h:"PV, PVC, StorageClass",b:"<p><b>PVC</b> — заявка пода на диск («нужно 20Gi, ReadWriteOnce»). <b>StorageClass</b> — как этот диск создать (тип диска в облаке). <b>PV</b> — сам созданный том.</p><pre>volumeClaimTemplates:\n  - metadata: { name: data }\n    spec:\n      accessModes: [ReadWriteOnce]\n      resources: { requests: { storage: 20Gi } }</pre>"},
 {h:"StatefulSet",b:"<p>Даёт: стабильные имена (<code>db-0, db-1</code>), стабильные тома, упорядоченный запуск и остановку. Это нужно кворумным системам, где узлы должны знать друг друга по именам.</p><p><b>Взрослый вопрос:</b> нужна ли вам БД в кластере? Managed-БД у провайдера снимает бэкапы, репликацию, обновления и ночные дежурства. В k8s держат stateful только с очень хорошей причиной.</p>"}],
 ch:[
 {t:"q",q:"Под с базой пересоздался — данные пропали. Чего не хватало?",a:["Реплик","PersistentVolumeClaim: без тома данные в эфемерном слое","Ingress","Probes"],c:1,why:"emptyDir живёт ровно столько, сколько под. Для данных — PVC с подходящим StorageClass."},
 {t:"q",q:"Почему для кластера Postgres берут StatefulSet, а не Deployment?",a:["Он быстрее","Нужны стабильные сетевые имена, свои тома и предсказуемый порядок запуска","Deployment не умеет тома","Так требует Helm"],c:1,why:"db-0 всегда db-0 и всегда со своим диском — на этом строится репликация."},
 {t:"q",q:"ReadWriteOnce означает…",a:["Один под может писать (том монтируется к одной ноде)","Только чтение","Один раз за жизнь","Только root"],c:1,why:"Для нескольких пишущих подов нужен ReadWriteMany (NFS/CephFS) — и это заметно дороже и медленнее."},
 {t:"q",q:"Когда БД в Kubernetes — оправданное решение?",a:["Всегда","Когда есть команда, экспертиза оператора и требования, которых managed не закрывает","Никогда","Для экономии"],c:1,why:"Честный ответ на собеседовании: «по умолчанию managed, в кластер — только с обоснованием». Это признак зрелости."}]}
);
W("obs").miss.push(
{id:"o4",t:"Логи как система",goal:"Искать по логам всего парка, а не ходить по серверам",
 cards:[
 {h:"Структурированные логи",b:"<pre>{\"ts\":\"2025-02-12T09:41:11Z\",\"level\":\"error\",\"service\":\"api\",\n \"trace_id\":\"a1b2c3\",\"msg\":\"db timeout\",\"duration_ms\":5012}</pre><p>JSON вместо свободного текста даёт фильтрацию по полям: «все ошибки сервиса api с duration &gt; 3s за час». По тексту так не поищешь.</p><p><code>trace_id</code> связывает лог с трейсом — из графика латентности проваливаешься в конкретный запрос.</p>"},
 {h:"Куда всё это едет",b:"<p>Приложение пишет в <b>stdout</b> → агент на ноде (Promtail/Fluent Bit/Vector) → хранилище (Loki, Elasticsearch, ClickHouse) → интерфейс (Grafana/Kibana).</p><p>Ограничивай объём: уровни логирования по окружениям, семплирование шумных событий, срок хранения. Логи — самая быстрорастущая статья счёта за наблюдаемость.</p>"}],
 ch:[
 {t:"q",q:"Куда контейнеризованное приложение должно писать логи?",a:["В файл внутри контейнера","В stdout/stderr","В базу","В syslog хоста"],c:1,why:"12-factor: приложение не занимается доставкой логов. Файлы внутри контейнера теряются при пересоздании."},
 {t:"q",q:"Зачем структурированные (JSON) логи?",a:["Красивее","Фильтрация и агрегация по полям, а не по подстроке","Меньше весят","Требование Docker"],c:1,why:"И стабильность: изменил текст сообщения — сломал поиск по подстроке; поля переживают рефакторинг."},
 {t:"q",q:"Счёт за хранение логов вырос втрое. Что сделаешь первым?",a:["Отключить логи","Найти самые шумные источники, снизить уровень, ввести семплирование и срок хранения","Купить больше места","Сжать вручную"],c:1,why:"Обычно 3-5 эндпоинтов или одна debug-строка в цикле дают 80% объёма."},
 {t:"q",q:"Что связывает лог, метрику и трейс одного запроса?",a:["Время","trace_id / correlation id","IP клиента","Имя сервиса"],c:1,why:"Сквозной идентификатор проносится через все сервисы (W3C traceparent). Без него в микросервисах расследование почти невозможно."}]},
{id:"o5",t:"Дежурство и runbook",goal:"Отвечать на алерт спокойно и по процедуре",
 cards:[
 {h:"Runbook",b:"<p>Для каждого алерта — короткий документ: что означает, как проверить, что уже пробовали, кого звать, как откатить.</p><pre>## Alert: HighErrorRate\nПроверить: dashboard «API / RED», доля 5xx\nЧастые причины: свежий деплой, деградация БД, лимит соединений\nБыстрые действия: rollout undo, скейл реплик\nЭскалация: команда бэкенда, канал #inc-api</pre><p>Runbook пишется <b>до</b> инцидента и правится после каждого — тогда следующий дежурный тратит минуты, а не часы.</p>"},
 {h:"Роли в инциденте",b:"<ul><li><b>Incident commander</b> — координирует, принимает решения, не чинит руками.</li><li><b>Operations</b> — исполняет действия.</li><li><b>Communications</b> — держит в курсе бизнес и поддержку.</li></ul><p>В маленькой команде это может быть один человек, но <b>роли всё равно проговаривают вслух</b> — иначе трое чинят одно и то же, а поддержка молчит.</p>"}],
 ch:[
 {t:"q",q:"Главная задача incident commander?",a:["Быстрее всех чинить","Координировать, вести таймлайн, решать — эскалировать или откатывать","Писать код","Общаться с прессой"],c:1,why:"Если командир ушёл руками в консоль, координация исчезает и инцидент затягивается."},
 {t:"q",q:"Когда пишется runbook?",a:["Во время инцидента","Заранее и обновляется после каждого разбора","После увольнения дежурного","Не пишется"],c:1,why:"Ночью в 4 утра ты не изобретаешь процедуру, а выполняешь её. В этом весь смысл."},
 {t:"q",q:"MTTR — это…",a:["Среднее время между сбоями","Среднее время восстановления","Число инцидентов","Время сборки"],c:1,why:"MTBF — между сбоями, MTTR — восстановления. Практика SRE говорит: снижать MTTR обычно дешевле и полезнее."},
 {t:"q",q:"Алерт разбудил, но по нему нечего делать. Что с ним сделать?",a:["Игнорировать впредь","Убрать из пейджинга: перевести в дашборд или удалить","Повысить приоритет","Добавить получателей"],c:1,why:"Каждый ночной алерт — обещание, что действие есть и оно срочное. Иначе доверие к системе алертов умирает."}]}
);
W("cloudsec").miss.push(
{id:"s4",t:"Бэкапы и аварийное восстановление",goal:"Уметь ответить «а если всё сгорит?»",
 cards:[
 {h:"Правило 3-2-1 и два числа",b:"<p><b>3</b> копии, <b>2</b> носителя, <b>1</b> вне площадки. Плюс: копия, из которой нельзя удалить (immutable/versioning) — защита от шифровальщика, который стирает и бэкапы.</p><p><b>RPO</b> — сколько данных допустимо потерять (частота бэкапов). <b>RTO</b> — за сколько обязаны восстановиться. Эти два числа определяют всю архитектуру резервирования и стоят денег.</p>"},
 {h:"Проверка",b:"<p>Бэкап без регулярного тестового восстановления — иллюзия. Раз в квартал: поднять из копии, сверить контрольные данные, замерить фактический RTO.</p><p>Отдельно: бэкап state Terraform, конфигураций, секретов (в защищённом хранилище) и <b>etcd</b>, если у тебя свой кластер Kubernetes.</p>"}],
 ch:[
 {t:"q",q:"RPO = 1 час означает…",a:["Восстановление за час","Допустимо потерять данные максимум за час","Бэкап хранится час","Час простоя"],c:1,why:"RPO определяет частоту копий, RTO — скорость восстановления. Путаница между ними — частая ошибка на собеседовании."},
 {t:"q",q:"Шифровальщик зашифровал прод и доступные ему бэкапы. Что должно было спасти?",a:["Антивирус","Неизменяемые копии вне периметра, недоступные из прод-окружения","Пароль посложнее","Больше реплик"],c:1,why:"Реплика — не бэкап: она мгновенно повторяет порчу данных. Нужны версии и офлайн/immutable-хранение."},
 {t:"q",q:"Что бэкапить в самоуправляемом кластере Kubernetes в первую очередь?",a:["Логи","etcd и PersistentVolume, плюс манифесты в git","Образы","Ingress"],c:1,why:"etcd — всё состояние кластера. Velero — стандартный инструмент для ресурсов и томов."},
 {t:"q",q:"Как понять, что бэкапы действительно работают?",a:["Смотреть, что задание зелёное","Регулярно восстанавливаться из копии и сверять данные","Проверять размер файла","Доверять провайдеру"],c:1,why:"«Зелёное задание» проверяет факт запуска, а не пригодность копии. Тестовое восстановление — единственное доказательство."}]}
);
