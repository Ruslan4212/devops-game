W("linux").miss.push(

{id:"l11",t:"Ссылки, архивы, специальные права",goal:"Разобраться в том, что выглядит магией",
 cards:[
 {h:"Inode: имя и содержимое — разные вещи",b:"<p>Файл в Linux состоит из двух частей: <b>inode</b> (запись с метаданными — права, владелец, размер, указатели на блоки данных) и <b>имя в каталоге</b>, которое на этот inode ссылается.</p><p>Каталог — это, по сути, таблица «имя → номер inode». Имя не является частью файла; это ярлык, лежащий в каталоге.</p><pre>ls -i file.txt    <span class='c'># номер inode</span>\nstat file.txt     <span class='c'># все метаданные разом</span></pre><p>Из этого устройства следует всё остальное: почему у файла может быть несколько имён, почему удаление — это операция над каталогом, и почему место не освобождается, пока файл открыт процессом.</p>"},
 {h:"Hard link и symlink",b:"<pre>ln file.txt hard        <span class='c'># жёсткая ссылка: второе имя того же inode</span>\nln -s /opt/app/v2 current <span class='c'># символическая: указатель на путь</span></pre><div class='tw'><table><tr><th></th><th>hard link</th><th>symlink</th></tr><tr><td>указывает на</td><td>inode (сами данные)</td><td>путь (строку с именем)</td></tr><tr><td>удалили оригинал</td><td>данные живы, доступны по второму имени</td><td>ссылка «повисает»</td></tr><tr><td>между разделами</td><td>невозможно</td><td>можно</td></tr><tr><td>на каталог</td><td>нельзя</td><td>можно</td></tr><tr><td>виден в ls</td><td>как обычный файл</td><td>как <code>l</code> со стрелкой</td></tr></table></div><p>Число во второй колонке <code>ls -l</code> — это количество жёстких ссылок на inode. Данные исчезают, только когда счётчик достигает нуля <b>и</b> файл не открыт ни одним процессом.</p><p>Именно поэтому <code>rm</code> называется «unlink»: он удаляет имя, а не файл.</p>"},
 {h:"Симлинк как механизм деплоя",b:"<pre>/opt/app/releases/2025-02-10/\n/opt/app/releases/2025-02-12/\n/opt/app/current -&gt; releases/2025-02-12</pre><p>Схема выкатки: распаковали новый релиз рядом, переключили ссылку, перезапустили сервис. Откат — переключить ссылку обратно, это доли секунды и не требует повторной сборки.</p><pre>ln -sfn /opt/app/releases/2025-02-10 /opt/app/current</pre><p><b>Флаги здесь принципиальны:</b> <code>-f</code> перезаписать существующую ссылку, <code>-n</code> не заходить внутрь неё.</p><p>Без <code>-n</code> команда трактует существующую ссылку-каталог как каталог и создаёт новую ссылку <i>внутри</i> него: получается <code>current/releases/...</code>. Ошибка коварна тем, что первый деплой проходит нормально, а ломается только второй.</p>"},
 {h:"tar: create, extract, list",b:"<pre>tar czf backup.tar.gz /var/www    <span class='c'># c=create z=gzip f=file</span>\ntar tzf backup.tar.gz | head      <span class='c'># t=list: посмотреть, не распаковывая</span>\ntar xzf backup.tar.gz -C /restore <span class='c'># x=extract, -C куда</span>\ntar czf b.tar.gz --exclude='*.log' /var/www</pre><p>Мнемоника: <b>c</b>reate, e<b>x</b>tract, lis<b>t</b>. Буква сжатия: <code>z</code> — gzip, <code>j</code> — bzip2, <code>J</code> — xz (лучше жмёт, медленнее).</p><p><b>Всегда сначала <code>t</code>.</b> Если архив собран без общего каталога внутри, распаковка разбросает сотни файлов по текущему каталогу вперемешку с существующими — разбирать придётся руками.</p><p>И вторая причина: <code>tar</code> сохраняет пути. Архив, созданный с абсолютным путём, при распаковке может попытаться перезаписать системные файлы. Современные версии вырезают ведущий слэш и предупреждают об этом.</p>"},
 {h:"Специальные биты",b:"<div class='tw'><table><tr><th>бит</th><th>цифра</th><th>что делает</th></tr><tr><td>SUID</td><td>4xxx</td><td>программа выполняется от имени <b>владельца файла</b>, а не запустившего</td></tr><tr><td>SGID</td><td>2xxx</td><td>на каталоге: новые файлы наследуют его группу</td></tr><tr><td>sticky</td><td>1xxx</td><td>в общем каталоге удалять файл может только его владелец</td></tr></table></div><p><b>SUID на практике:</b> так работает <code>passwd</code>. Обычный пользователь меняет свой пароль, то есть пишет в <code>/etc/shadow</code>, доступный только root. Бит SUID и владелец root дают программе нужные права на время выполнения.</p><p><b>Sticky на практике:</b> права <code>/tmp</code> выглядят как <code>drwxrwxrwt</code>. Писать могут все, но удалять — только свои файлы. Без этого бита любой пользователь удалял бы чужие временные файлы, ведь для удаления нужны права на <i>каталог</i>, а не на файл.</p><pre>find / -perm -4000 -type f 2&gt;/dev/null   <span class='c'># все SUID-файлы в системе</span></pre><p>Этот список — обязательный пункт аудита: уязвимость в SUID-программе означает локальное повышение привилегий до root.</p>"},
 {h:"Почему удаление файла не освобождает место",b:"<p>Собираем всё вместе. Место освобождается, когда выполнены <b>два</b> условия: удалено последнее имя (счётчик ссылок ноль) <i>и</i> файл не открыт ни одним процессом.</p><p>Если сервис держит открытым лог, а файл удалили, ядро сохраняет данные до закрытия дескриптора. Именно так возникает расхождение между <code>df</code> (место занято) и <code>du</code> (файлов нет).</p><pre>lsof | grep deleted       <span class='c'># найти такие файлы</span>\nls -l /proc/PID/fd        <span class='c'># дескрипторы конкретного процесса</span></pre><p>Лечится перезапуском процесса или, если он поддерживает, сигналом на переоткрытие логов.</p>"}],
 ch:[
 {t:"q",d:1,q:"Ты удалил файл, на который указывала символическая ссылка. Что стало со ссылкой и что было бы с жёсткой ссылкой?",a:["Обе перестают работать одинаково","Символическая ссылка осталась, но указывает в никуда; жёсткая ссылка сохранила бы доступ к данным, потому что ссылается на inode, а не на путь","Символическая ссылка удалилась вместе с файлом","Данные восстановятся автоматически"],c:1,why:"Symlink хранит строку с путём, hard link — указатель на inode. Данные исчезают, только когда обнулился счётчик жёстких ссылок и файл никем не открыт."},
 {t:"c",d:2,q:"Тебе прислали архив backup.tar.gz. Прежде чем распаковывать, посмотри его содержимое, ничего не извлекая.",ans:["tar tzf backup.tar.gz","tar -tzf backup.tar.gz","tar tf backup.tar.gz"],hint:"t означает list",why:"Проверка показывает, лежат ли файлы внутри общего каталога или разлетятся по текущему. Второй случай превращает распаковку в уборку вручную среди сотен файлов."},
 {t:"c",d:2,q:"Создай символическую ссылку /opt/app/current, указывающую на каталог /opt/app/releases/v2.",ans:["ln -s /opt/app/releases/v2 /opt/app/current"],hint:"ln -s сначала цель, потом имя ссылки",why:"Порядок аргументов такой же, как у cp: сначала откуда, потом куда. Перепутав, получишь ссылку в неожиданном месте либо ошибку о существующем файле."},
 {t:"q",d:3,q:"Права каталога <code>/tmp</code> выглядят как <code>drwxrwxrwt</code>. Что означает последняя буква и какую проблему она решает?",a:["Это опечатка в выводе ls","Sticky bit: писать в каталог могут все, но удалять файл — только его владелец; без него любой удалял бы чужие файлы, ведь для удаления нужны права на каталог","Каталог доступен только для чтения","Файлы в нём автоматически удаляются"],c:1,why:"Ключ к пониманию — что удаление файла является изменением каталога, а не файла. Sticky bit добавляет исключение из этого правила для общих каталогов."},
 {t:"q",d:4,q:"Скрипт деплоя выполняет <code>ln -sf /opt/app/releases/new /opt/app/current</code>. Первый деплой прошёл нормально, после второго появился путь <code>current/new/new</code>. Что не так и как исправить?",a:["Нужно удалять ссылку перед созданием новой","Не хватает флага <code>-n</code>: без него ln заходит внутрь существующей ссылки-каталога и создаёт ссылку там — правильная форма <code>ln -sfn</code>","Нужно использовать жёсткую ссылку","Ошибка в правах на каталог"],c:1,why:"Коварство в том, что первый запуск работает: ссылки ещё нет, и она просто создаётся. Проблема проявляется со второго деплоя, когда цель уже существует."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Аудит обнаружил в системе бинарник <code>/opt/tools/backup</code> с правами <code>-rwsr-xr-x root root</code>. Разработчик объясняет: «иначе скрипт бэкапа не мог читать файлы других пользователей». Чем это опасно и что предложить взамен?",a:["Ничего страшного, так работает и системная команда passwd","Любой пользователь выполняет этот код от имени root: любая ошибка в программе становится способом получить полный контроль над сервером. Правильно — точечное правило в sudoers на конкретную команду или сервисный пользователь в нужной группе","Достаточно убрать право чтения для остальных","Нужно перенести бинарник в /usr/local/bin"],c:1,why:"SUID оправдан для нескольких проверенных системных утилит, прошедших годы аудита. Собственный скрипт с этим битом — это раздача root всем пользователям машины ради удобства одной задачи."}]},

{id:"l12",t:"Процессы вглубь: состояния и лимиты",goal:"Читать состояние системы, а не гадать",
 cards:[
 {h:"Состояния процессов",b:"<div class='tw'><table><tr><th>код</th><th>состояние</th><th>что значит на практике</th></tr><tr><td><b>R</b></td><td>running</td><td>выполняется или готов выполняться</td></tr><tr><td><b>S</b></td><td>sleeping</td><td>ждёт события — нормальное состояние большинства процессов</td></tr><tr><td><b>D</b></td><td>uninterruptible sleep</td><td>ждёт диск или сеть; <b>не убивается даже -9</b></td></tr><tr><td><b>Z</b></td><td>zombie</td><td>завершился, родитель не забрал код возврата</td></tr><tr><td><b>T</b></td><td>stopped</td><td>приостановлен сигналом</td></tr></table></div><p><b>Состояние D</b> означает, что процесс находится внутри системного вызова, который нельзя прервать: ядро ждёт ответа от устройства. Сигнал будет доставлен только после завершения операции. Массовые D-процессы — признак умирающего диска или отвалившегося сетевого хранилища; лечить надо носитель, а не процессы.</p><p><b>Зомби</b> сам по себе почти не потребляет ресурсов: от него осталась только запись в таблице процессов с кодом возврата. Но накопление зомби означает, что родитель не вызывает wait — то есть сломан. В контейнерах это решается процессом-init (tini, флаг <code>--init</code>), который усыновляет и «хоронит» осиротевших потомков.</p>"},
 {h:"Приоритеты: nice и ionice",b:"<pre>nice -n 10 ./heavy.sh    <span class='c'># запустить с пониженным приоритетом</span>\nrenice -n 5 -p 4210      <span class='c'># изменить у работающего процесса</span>\nionice -c3 -p 4210       <span class='c'># понизить приоритет ввода-вывода</span></pre><p>Шкала nice: от <b>−20</b> (самый приоритетный, доступно только root) до <b>19</b> (самый уступчивый). Значение по умолчанию 0. Число называется «nice» именно потому, что чем оно больше, тем «вежливее» процесс уступает другим.</p><p>Практика: тяжёлые фоновые задачи — бэкап, переиндексация, архивирование — запускают с nice 10–19, чтобы они не мешали обслуживать пользователей.</p><p>Важная деталь: nice влияет только на конкуренцию за процессор. Если задача упирается в диск, нужен <code>ionice</code>, а если в сеть — ограничение на уровне приложения или трафика.</p>"},
 {h:"cgroups: чем ядро ограничивает ресурсы",b:"<p><b>cgroups</b> (control groups) — механизм ядра, ограничивающий и учитывающий ресурсы группы процессов: процессор, память, ввод-вывод, число процессов.</p><p>На нём построено всё современное:</p><ul><li><code>MemoryMax=512M</code> и <code>CPUQuota=50%</code> в systemd-юните</li><li><code>--memory</code> и <code>--cpus</code> у контейнера Docker</li><li><code>resources.limits</code> в манифесте Kubernetes</li></ul><p>Когда ты ставишь лимит памяти поду, ты в конечном счёте настраиваешь cgroup. Понимание этого объясняет поведение: <b>превышение лимита памяти = мгновенное убийство процесса</b> (OOM), <b>превышение лимита CPU = троттлинг</b>, то есть процессу просто выделяют меньше времени.</p><p>Вместе с <b>namespaces</b> (что процесс видит: свои PID, сеть, точки монтирования) cgroups (сколько ему можно) и составляют контейнер. Контейнер — не виртуальная машина, а обычный процесс с этими двумя ограничениями.</p>"},
 {h:"OOM killer",b:"<p>Когда память системы заканчивается, ядро выбирает жертву и убивает её, чтобы спасти остальное. Выбор основан на «очках» — грубо, кто больше потребляет, тот и кандидат.</p><pre>dmesg -T | grep -i \"killed process\"\njournalctl -k | grep -i oom</pre><p>Характерный признак: приложение исчезло без записи в собственном логе. Оно и не могло ничего написать — SIGKILL не перехватывается.</p><p>Два разных сценария, которые важно различать:</p><ul><li><b>OOM всей системы</b> — память кончилась на хосте, ядро убивает самого крупного потребителя, возможно, вообще не виновника</li><li><b>OOM внутри cgroup</b> — процесс превысил свой лимит; страдает только он, остальным ничего не грозит</li></ul><p>Второй сценарий — то, что происходит с контейнером при превышении memory limit, и он гораздо предпочтительнее первого.</p>"},
 {h:"Лимиты пользователя и дескрипторы",b:"<pre>ulimit -n        <span class='c'># лимит открытых файлов в текущей сессии</span>\nulimit -a        <span class='c'># все лимиты</span>\ncat /proc/PID/limits   <span class='c'># лимиты конкретного процесса</span>\nls /proc/PID/fd | wc -l  <span class='c'># сколько дескрипторов открыто сейчас</span></pre><p>Ошибка <code>Too many open files</code> означает, что процесс упёрся в лимит файловых дескрипторов. Важно: <b>сетевые соединения тоже дескрипторы</b> — на нагруженном сервере лимит выбирается именно ими, а не файлами.</p><p><b>Где чинить.</b> <code>ulimit</code> в сессии действует только до выхода и не влияет на сервис, запущенный systemd. Правильное место — юнит: <code>LimitNOFILE=65535</code>. Для контейнеров — настройки среды выполнения.</p><p>Изменение лимита без понимания причины опасно: иногда рост числа дескрипторов означает утечку соединений, и увеличение лимита лишь отодвигает падение.</p>"},
 {h:"Порядок разбора «процесс ведёт себя странно»",b:"<ol><li><code>ps aux | grep имя</code> — состояние, потребление, полная команда</li><li><code>cat /proc/PID/status</code> — детали: память, потоки, родитель</li><li><code>ls /proc/PID/fd | wc -l</code> и <code>cat /proc/PID/limits</code> — не упёрся ли в лимиты</li><li><code>dmesg -T | tail</code> — не убивало ли его ядро</li><li><code>strace -p PID</code> (осторожно, замедляет) — какие системные вызовы делает</li></ol><p>Каталог <code>/proc/PID/</code> — самый недооценённый инструмент диагностики: там видно окружение процесса (<code>environ</code>), его рабочий каталог (<code>cwd</code>), запущенный бинарник (<code>exe</code>) и все открытые файлы (<code>fd</code>).</p>"}],
 ch:[
 {t:"q",d:1,q:"В выводе <code>ps aux</code> процесс находится в состоянии <code>Z</code>. Что это значит и опасно ли это само по себе?",a:["Процесс заблокирован и потребляет процессор","Зомби: процесс завершился, но родитель не забрал код возврата — сам по себе безвреден, однако накопление зомби означает сломанного родителя","Процесс приостановлен пользователем","Наивысший приоритет выполнения"],c:1,why:"От зомби остаётся только запись в таблице процессов. Проблема не в ресурсах, а в том, что родительский процесс не вызывает wait — в контейнерах это лечится процессом-init."},
 {t:"c",d:2,q:"Nginx работает, и ты изменил его конфигурацию. Отправь процессу 711 сигнал, по которому демоны перечитывают конфиг без перезапуска.",ans:["kill -1 711","kill -hup 711","kill -sighup 711","kill -s hup 711"],hint:"сигнал номер 1 или его имя HUP",why:"SIGHUP исторически означал разрыв связи с терминалом, и демоны стали использовать его как команду «перечитай настройки». Соединения при этом не рвутся."},
 {t:"q",d:3,q:"Процесс висит в состоянии <code>D</code> и не завершается даже после <code>kill -9</code>. Объясни, почему сигнал не действует и куда смотреть.",a:["У тебя недостаточно прав, нужен root","Процесс в непрерываемом сне внутри системного вызова: ядро ждёт ответа от диска или сети, и сигнал будет доставлен только после завершения операции — проверять надо носитель или сетевое хранилище","Это баг ядра, поможет только перезагрузка","Процесс защищён от сигналов настройкой"],c:1,why:"SIGKILL нельзя перехватить, но его можно только доставить — а доставка происходит при выходе из системного вызова. Массовые D-процессы почти всегда означают проблему с устройством хранения."},
 {t:"q",d:4,q:"Приложение периодически исчезает без единой записи в собственном логе. Где искать причину и почему в логе приложения ничего нет?",a:["В логах веб-сервера","В <code>dmesg -T | grep -i \"killed process\"</code> — вероятно, процесс убил OOM killer сигналом SIGKILL, который невозможно перехватить, поэтому приложение не успело ничего записать","В журнале аутентификации","В настройках ротации логов"],c:1,why:"Отсутствие предсмертной записи — сам по себе диагностический признак: приложение, падающее по своей ошибке, обычно успевает написать трассировку. Молчаливое исчезновение указывает на внешнее убийство."},
 {t:"q",d:4,q:"Сервис под нагрузкой падает с ошибкой <code>Too many open files</code>. Коллега предлагает выполнить <code>ulimit -n 65535</code> в своей сессии и перезапустить сервис. Сработает ли это и как правильно?",a:["Да, это стандартное решение","Нет: ulimit действует только в текущей сессии и не влияет на сервис, запущенный systemd — нужна директива <code>LimitNOFILE=65535</code> в юните; и стоит проверить, не утечка ли соединений вызывает рост числа дескрипторов","Нужно перезагрузить сервер после ulimit","Достаточно увеличить лимит в /etc/security/limits.conf"],c:1,why:"Сервисы systemd получают лимиты из юнита, а не из чьей-то интерактивной сессии. И вторая половина ответа важнее первой: если дескрипторы утекают, увеличение лимита лишь отложит падение."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Под в Kubernetes регулярно перезапускается, в событиях <code>OOMKilled</code>, код выхода 137. Разработчик возражает: «на ноде свободно 12 ГБ памяти, это баг кластера». Объясни, что происходит на уровне ядра и кто прав.",a:["Разработчик прав, нужно обновить кластер","Свободная память ноды здесь не при чём: контейнер превысил лимит своей cgroup, и ядро убило процесс внутри этой группы — поднимать нужно limits или разбираться с потреблением приложения","Проблема в сетевом плагине кластера","Нужно перезапустить kubelet на ноде"],c:1,why:"Лимит пода — это ограничение cgroup, а не доля от памяти ноды. 137 = 128 + 9, то есть SIGKILL. Отсюда же разница с CPU: превышение процессорного лимита даёт троттлинг, а памяти — немедленное убийство."}]},

{id:"l13",t:"Диски, монтирование, LVM",goal:"Добавить место, не уронив сервер",
 cards:[
 {h:"Что где лежит",b:"<pre>lsblk               <span class='c'># дерево дисков, разделов и точек монтирования</span>\nlsblk -f            <span class='c'># с типами ФС и UUID</span>\ndf -h               <span class='c'># занятость смонтированных ФС</span>\ndf -i               <span class='c'># занятость inode</span>\nblkid               <span class='c'># UUID разделов</span>\nmount | column -t   <span class='c'># что и как смонтировано сейчас</span></pre><p><code>lsblk</code> отвечает на первый вопрос при работе с дисками: какие устройства есть, как они разбиты и что куда смонтировано. Это карта, с которой начинается любая операция.</p>"},
 {h:"Два способа кончиться месту",b:"<p><b>Байты.</b> Обычный случай: <code>df -h</code> показывает 100%. Ищем виновника через <code>du -sh /var/* | sort -h</code>.</p><p><b>Inode.</b> Коварный случай: <code>df -h</code> показывает свободное место, а система пишет «No space left on device». Количество inode фиксируется при создании файловой системы, и миллионы мелких файлов исчерпывают лимит раньше, чем байты.</p><pre>df -i\nfind /var -xdev -type f | wc -l          <span class='c'># сколько файлов</span>\nfind /var -xdev -type d -exec sh -c 'echo $(ls -A \"$1\" | wc -l) \"$1\"' _ {} \\; | sort -rn | head</pre><p>Типичные источники: файлы сессий PHP, кэш, очереди писем, распакованные архивы с миллионами мелких файлов.</p><p>Лечится удалением мелочи; радикально — пересозданием ФС с другими параметрами, что означает даунтайм. Поэтому лучше заметить заранее: алерт стоит ставить и на inode тоже.</p>"},
 {h:"fstab и постоянное монтирование",b:"<pre># /etc/fstab\nUUID=9f2c-1ab4  /data  ext4  defaults,noatime  0 2\n#  что            куда   тип    опции         dump fsck</pre><p><b>Монтируй по UUID, а не по <code>/dev/sdb1</code>.</b> Имена устройств зависят от порядка обнаружения дисков и меняются при добавлении нового диска — раздел, который был sdb1, становится sdc1, и система монтирует не то.</p><p>Опция <code>noatime</code> отключает запись времени последнего доступа при каждом чтении — заметно снижает нагрузку на диск для файловых серверов и баз.</p><p><b>Правило выживания:</b> после правки fstab обязательно выполни <code>mount -a</code>. Если строка ошибочна, ты узнаешь об этом сейчас, а не при следующей загрузке, когда сервер уйдёт в аварийный режим и перестанет пускать по SSH. Восстановление в этом случае — только через консоль провайдера.</p>"},
 {h:"LVM: место без перезагрузки",b:"<p>LVM добавляет слой абстракции между физическими дисками и файловой системой:</p><pre>физические диски (PV) → группа томов (VG) → логические тома (LV) → ФС</pre><pre>pvcreate /dev/sdc            <span class='c'># сделать диск физическим томом</span>\nvgextend vg0 /dev/sdc        <span class='c'># добавить в группу</span>\nlvextend -L +50G /dev/vg0/data\nresize2fs /dev/vg0/data      <span class='c'># растянуть ext4</span>\nxfs_growfs /data             <span class='c'># растянуть xfs (по точке монтирования)</span>\n\nvgs; lvs; pvs                <span class='c'># обзор состояния</span></pre><p><b>Два слоя — два действия.</b> Расширил логический том, но забыл файловую систему — место «есть» в <code>lvs</code>, но <code>df</code> его не видит. Это самая частая ошибка при работе с LVM.</p><p>Современные версии умеют делать оба шага сразу: <code>lvextend -r -L +50G ...</code> (флаг <code>-r</code> означает resize filesystem).</p>"},
 {h:"Снапшоты LVM",b:"<pre>lvcreate -L 5G -s -n db_snap /dev/vg0/db   <span class='c'># снимок</span>\nlvconvert --merge /dev/vg0/db_snap         <span class='c'># откат к снимку</span>\nlvremove /dev/vg0/db_snap                  <span class='c'># удалить</span></pre><p>Снапшот делается мгновенно и занимает место только под изменения. Его берут перед рискованным обновлением или миграцией БД: если что-то пошло не так, откат занимает секунды вместо восстановления из бэкапа за часы.</p><p><b>Снапшот — не бэкап.</b> Он живёт на том же диске и исчезнет вместе с ним. Это инструмент короткого отката, а не резервного копирования. Ещё одна тонкость: если снапшот переполнится (изменений больше выделенного места), он станет недействительным.</p>"},
 {h:"Расширение под нагрузкой",b:"<p>Хорошая новость: расширение LVM и современных файловых систем (ext4, xfs) выполняется <b>онлайн</b>, на смонтированном томе, без остановки сервисов.</p><p>Порядок при заполнении диска на проде:</p><ol><li>убедиться, что в группе томов есть свободное место (<code>vgs</code>) или добавить диск</li><li><code>lvextend -r -L +50G /dev/vg0/data</code></li><li>проверить <code>df -h</code></li><li>только после стабилизации разбираться, почему выросло потребление</li></ol><p>Уменьшение тома, в отличие от расширения, для xfs невозможно вовсе, а для ext4 требует размонтирования. Поэтому размеры лучше наращивать постепенно, а не «с запасом на всякий случай».</p>"}],
 ch:[
 {t:"c",d:1,q:"Ты подключился к незнакомому серверу и хочешь увидеть все диски, их разделы и точки монтирования одной командой.",ans:["lsblk","lsblk -f"],hint:"list block devices",why:"lsblk даёт карту хранилища за одну команду. С флагом -f добавляются типы файловых систем и UUID, которые понадобятся при правке fstab."},
 {t:"q",d:2,q:"<code>df -h</code> показывает, что раздел занят на 40%, но приложение падает с ошибкой «No space left on device». Что проверить следующей командой?",a:["Права доступа к каталогу","<code>df -i</code> — возможно, закончились inode: их количество фиксировано, и миллионы мелких файлов исчерпывают лимит раньше байтов","Свободную оперативную память","Сетевые соединения"],c:1,why:"Типичные источники — файлы сессий, кэш и очереди. Поэтому алерт на диск должен следить и за байтами, и за inode: второй случай без этого обнаруживается уже по факту аварии."},
 {t:"c",d:2,q:"Ты добавил новую строку в /etc/fstab. Проверь корректность всех записей и смонтируй их прямо сейчас, не перезагружая сервер.",ans:["mount -a","sudo mount -a"],hint:"mount с флагом all",why:"Единственная защита от «сервер не поднялся после перезагрузки из-за одной строки в fstab». Ошибка в этом файле отправляет систему в аварийный режим без доступа по SSH."},
 {t:"q",d:3,q:"Почему в fstab указывают UUID вместо привычного <code>/dev/sdb1</code>?",a:["UUID короче и удобнее читать","Имена устройств зависят от порядка обнаружения дисков и меняются при добавлении нового — система смонтирует не тот раздел или не загрузится, а UUID привязан к самой файловой системе","Так требует ext4","С UUID монтирование выполняется быстрее"],c:1,why:"Добавили диск — порядок сместился, и sdb1 стал другим разделом. Это одна из тех ошибок, которая проявляется не сразу, а при следующей перезагрузке через полгода."},
 {t:"q",d:4,q:"Ты расширил логический том LVM на 50 ГБ, команда отработала без ошибок, но <code>df -h</code> показывает прежний размер. Что пропущено?",a:["Нужно перезагрузить сервер","Не растянута файловая система: после lvextend нужен <code>resize2fs</code> для ext4 или <code>xfs_growfs</code> для xfs — либо сразу флаг <code>-r</code> у lvextend","Нужно перемонтировать том","Изменения применятся через несколько минут"],c:1,why:"LVM и файловая система — независимые слои: том стал больше, но ФС по-прежнему считает своим прежний размер. Обе операции выполняются онлайн, без остановки сервисов."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Ночью раздел с базой данных заполнился, СУБД остановилась. В группе томов есть 100 ГБ нераспределённого места. Нужно вернуть сервис максимально быстро и без остановки. Опиши правильный порядок.",a:["Перезагрузить сервер и надеяться, что место освободится","Проверить <code>vgs</code>, выполнить <code>lvextend -r -L +50G</code> на томе базы — расширение идёт онлайн на смонтированном томе, затем убедиться по <code>df -h</code> и запустить СУБД; после стабилизации разобраться с причиной роста и поставить алерт на 80%","Удалить журналы базы данных, чтобы освободить место","Скопировать базу на другой сервер и переключить приложение"],c:1,why:"Главный практический аргумент за LVM: место добавляется под нагрузкой, без размонтирования и даунтайма. Удаление журналов базы — опасный путь: можно лишиться возможности восстановления на момент времени."}]},

{id:"l14",t:"Логи системы и ротация",goal:"Не утонуть в логах и не потерять нужное",
 cards:[
 {h:"Две системы журналирования",b:"<div class='tw'><table><tr><th></th><th>journald</th><th>rsyslog</th></tr><tr><td>формат</td><td>бинарный, структурированный</td><td>текстовые файлы</td></tr><tr><td>фильтры</td><td>по юниту, приоритету, времени, полям</td><td>grep по файлу</td></tr><tr><td>пересылка</td><td>ограниченно</td><td>штатно, на удалённый сервер</td></tr><tr><td>где</td><td><code>journalctl</code></td><td><code>/var/log/*</code></td></tr></table></div><p>На современных серверах работают обе: journald собирает всё от systemd-юнитов, rsyslog дублирует часть в текстовые файлы и умеет отправлять наружу. Поэтому одно и то же сообщение можно встретить и там, и там.</p>"},
 {h:"journalctl: основные фильтры",b:"<pre>journalctl -u nginx                      <span class='c'># конкретный юнит</span>\njournalctl -u nginx -f                   <span class='c'># живой поток</span>\njournalctl -u api --since \"1 hour ago\"\njournalctl --since \"09:30\" --until \"10:00\"\njournalctl -p err -b                     <span class='c'># только ошибки текущей загрузки</span>\njournalctl -k                            <span class='c'># сообщения ядра</span>\njournalctl -b -1                         <span class='c'># журнал предыдущей загрузки</span>\njournalctl --disk-usage\njournalctl --vacuum-time=7d              <span class='c'># удалить старше недели</span></pre><p>Уровни для <code>-p</code>: emerg, alert, crit, err, warning, notice, info, debug.</p><p><code>journalctl -b -1</code> особенно ценен после внезапной перезагрузки: он показывает, что происходило перед тем, как машина ушла в ребут. Обычный <code>-b</code> покажет уже новую загрузку, где интересного нет.</p>"},
 {h:"logrotate",b:"<pre># /etc/logrotate.d/myapp\n/var/log/myapp/*.log {\n  daily\n  rotate 14\n  size 100M\n  compress\n  delaycompress\n  missingok\n  notifempty\n  create 0640 app app\n  sharedscripts\n  postrotate\n    systemctl reload myapp &gt; /dev/null 2&gt;&amp;1 || true\n  endscript\n}</pre><div class='tw'><table><tr><th>опция</th><th>смысл</th></tr><tr><td><code>rotate 14</code></td><td>хранить 14 архивов</td></tr><tr><td><code>daily</code> / <code>size 100M</code></td><td>вращать по времени и по размеру</td></tr><tr><td><code>compress</code></td><td>сжимать старые файлы</td></tr><tr><td><code>delaycompress</code></td><td>не сжимать самый свежий — он может быть ещё открыт</td></tr><tr><td><code>missingok</code></td><td>не ругаться, если файла нет</td></tr><tr><td><code>create</code></td><td>с какими правами создавать новый файл</td></tr></table></div><pre>logrotate -d /etc/logrotate.d/myapp   <span class='c'># сухой прогон, ничего не делает</span>\nlogrotate -f /etc/logrotate.d/myapp   <span class='c'># принудительная ротация</span></pre>"},
 {h:"Две стратегии ротации и почему это важно",b:"<p><b>Переименование (по умолчанию).</b> logrotate переименовывает <code>app.log</code> в <code>app.log.1</code> и создаёт новый пустой файл. Но процесс продолжает писать по <b>дескриптору</b>, который указывает на переименованный, а затем удалённый файл.</p><p>Результат: новый файл пуст, старый занимает место и недоступен. <code>df</code> показывает переполнение, <code>du</code> ничего не находит — та самая ситуация с удалённым открытым файлом.</p><p>Два способа решить:</p><ul><li><b><code>postrotate</code></b> с сигналом сервису, чтобы он переоткрыл файлы — правильный путь, если приложение это умеет</li><li><b><code>copytruncate</code></b> — logrotate копирует содержимое и обнуляет исходный файл, не меняя inode. Приложение продолжает писать в тот же дескриптор. Цена — крошечное окно между копированием и обнулением, в котором несколько строк могут потеряться</li></ul><p>Выбор зависит от приложения: если оно понимает сигнал (nginx, postgres) — postrotate; если нет — copytruncate.</p>"},
 {h:"Уровни логирования и цена",b:"<p>На проде обычно <code>info</code>. Уровень <code>debug</code> включают точечно и временно.</p><p><b>Две причины не оставлять debug надолго:</b></p><ol><li><b>Объём.</b> Debug легко даёт кратный рост, забивает диск и раздувает счёт за хранение.</li><li><b>Безопасность.</b> В отладочный вывод регулярно попадают тела запросов, токены, заголовки авторизации и персональные данные. Лог с секретом читают шире, чем боевую базу: он уезжает в централизованное хранилище, в бэкапы и на экраны разработчиков.</li></ol><p>Ограничивать объём нужно заранее: <code>SystemMaxUse=1G</code> в <code>/etc/systemd/journald.conf</code>, срок хранения в системе сбора, семплирование шумных событий.</p>"},
 {h:"Что должно быть в хорошей записи лога",b:"<ul><li>отметка времени с часовым поясом (лучше UTC — иначе при разборе инцидента в распределённой системе придётся пересчитывать)</li><li>уровень</li><li>имя сервиса и экземпляра</li><li>сквозной идентификатор запроса, чтобы связать записи разных сервисов</li><li>суть события и контекст (какой пользователь, какой ресурс)</li></ul><p>И то, чего быть не должно: пароли, токены, номера карт, полные тела запросов с персональными данными.</p>"}],
 ch:[
 {t:"c",d:1,q:"Посмотри журнал сервиса nginx через systemd.",ans:["journalctl -u nginx"],hint:"journalctl и флаг юнита",why:"Флаг -u фильтрует по конкретному юниту. Дальше добавляют -f для живого потока и --since для сужения окна времени."},
 {t:"c",d:2,q:"Нужно быстро увидеть, что вообще сломано в системе: покажи только ошибки из журнала за текущую загрузку.",ans:["journalctl -p err -b","journalctl -b -p err","journalctl -p 3 -b"],hint:"-p задаёт приоритет, -b ограничивает текущей загрузкой",why:"Это способ за одну команду получить список проблем, не читая мегабайты информационных сообщений. Хорошая первая команда на незнакомом сервере."},
 {t:"q",d:3,q:"Сервер внезапно перезагрузился ночью, и ты хочешь понять, что происходило перед этим. Обычный <code>journalctl -b</code> показывает уже новую загрузку. Что использовать?",a:["Ничего, данные потеряны при перезагрузке","<code>journalctl -b -1</code> — журнал предыдущей загрузки, если включено постоянное хранение журнала","Только /var/log/syslog","Восстановить журнал из бэкапа"],c:1,why:"Важная деталь: по умолчанию в некоторых системах журнал хранится только в памяти и теряется при перезагрузке. Постоянное хранение включается созданием каталога /var/log/journal — это стоит сделать заранее, до первого инцидента."},
 {t:"q",d:4,q:"Зачем в конфигурации logrotate используют опцию <code>copytruncate</code> и какова её цена?",a:["Она ускоряет ротацию больших файлов","Для приложений, которые не умеют переоткрывать лог по сигналу: файл копируется и обнуляется на месте, inode не меняется, поэтому процесс продолжает писать туда же. Цена — небольшое окно, в котором несколько строк могут потеряться","Она включает сжатие архивов","Она удаляет старые архивы автоматически"],c:1,why:"Без этого процесс продолжит писать в переименованный и затем удалённый файл: новый лог пуст, место занято, df и du расходятся. Более чистая альтернатива — postrotate с сигналом, если приложение его понимает."},
 {t:"q",d:4,q:"На проде месяц работает уровень логирования debug. Помимо объёма, чем это опасно?",a:["Ничем существенным, только место на диске","В отладочный вывод попадают тела запросов, токены и персональные данные, а логи доступны шире, чем боевая база: уезжают в централизованное хранилище, в бэкапы и на экраны разработчиков","Замедляется работа диска","Ломается ротация логов"],c:1,why:"Секрет, попавший в лог, считается скомпрометированным — его нужно ротировать, а не просто удалить запись. Именно поэтому debug на проде включают точечно и с явным сроком."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Диск заполнен логами. Ты удалил старые файлы командой <code>rm /var/log/app/*.log.1</code>, но <code>df -h</code> по-прежнему показывает 100%, а <code>du</code> насчитывает вдвое меньше. Объясни механику и назови действия — немедленные и системные.",a:["Файловая система повреждена, нужен fsck и перезагрузка","Процесс держит открытыми удалённые файлы, и ядро не освобождает блоки: найти через <code>lsof | grep deleted</code>, перезапустить сервис (или послать ему сигнал переоткрытия). Системно — настроить logrotate с copytruncate или postrotate и алерт на 80% заполнения","Нужно удалить ещё больше файлов","Помогут только диски большего объёма"],c:1,why:"Расхождение df и du — точный признак этой ситуации. Причина всегда системная: ротация настроена неправильно или её нет вовсе, а ручное удаление логов лишь повторяет проблему через месяц."}]},

{id:"l15",t:"Сеть изнутри",goal:"Понять, докуда доходит пакет",
 cards:[
 {h:"Адреса, маршруты, резолвинг",b:"<pre>ip a                 <span class='c'># адреса интерфейсов</span>\nip r                 <span class='c'># таблица маршрутизации</span>\nip r get 8.8.8.8     <span class='c'># каким маршрутом пойдёт конкретный пакет</span>\nip -s link           <span class='c'># статистика и ошибки на интерфейсах</span>\nresolvectl status    <span class='c'># какие DNS-серверы используются</span>\ncat /etc/resolv.conf</pre><p>В выводе <code>ip r</code> ищи строку <b>default</b> — это шлюз по умолчанию. Его отсутствие означает «наружу пакеты не уйдут», хотя интерфейс поднят и локальная сеть работает.</p><p><code>ifconfig</code> и <code>netstat</code> устарели и на многих системах вообще не установлены. Их заменили <code>ip</code> и <code>ss</code> — использование современных команд на собеседовании читается как признак актуального опыта.</p>"},
 {h:"Диагностика по слоям — снизу вверх",b:"<div class='tw'><table><tr><th>слой</th><th>инструмент</th><th>что проверяет</th></tr><tr><td>имя</td><td><code>dig</code>, <code>getent hosts</code></td><td>резолвится ли домен</td></tr><tr><td>L3 связность</td><td><code>ping</code></td><td>доходит ли пакет до хоста</td></tr><tr><td>маршрут</td><td><code>traceroute</code>, <code>mtr</code></td><td>где теряется и на каком узле</td></tr><tr><td>L4 порт</td><td><code>nc -zv</code>, <code>ss -tulpn</code></td><td>слушает ли сервис, доходит ли до порта</td></tr><tr><td>TLS</td><td><code>openssl s_client</code>, <code>curl -v</code></td><td>сертификат и рукопожатие</td></tr><tr><td>L7</td><td><code>curl -I</code></td><td>код ответа и заголовки</td></tr></table></div><p>Двигаясь снизу вверх, ты за пять команд сужаешь проблему до одного слоя вместо перебора гипотез. Каждая проверка отсекает половину пространства поиска — это тот же бинарный поиск, что и в git bisect.</p>"},
 {h:"Refused против timeout — ключевая развилка",b:"<div class='tw'><table><tr><th>ответ</th><th>что произошло</th><th>куда идти</th></tr><tr><td><b>Connection refused</b></td><td>пакет дошёл, но порт никто не слушает — хост активно отказал</td><td>к сервису: запущен ли, на том ли адресе слушает</td></tr><tr><td><b>Connection timed out</b></td><td>ответа нет вовсе: пакет отброшен молча</td><td>к сети: фаервол, security group, маршрут</td></tr><tr><td><b>No route to host</b></td><td>маршрут отсутствует</td><td>к таблице маршрутизации</td></tr><tr><td><b>Name or service not known</b></td><td>имя не разрешилось</td><td>к DNS, до сети дело не дошло</td></tr></table></div><p>Различие между refused и timeout — самая полезная развилка в сетевой диагностике. Первое означает, что связность есть и проблема в сервисе; второе — что пакеты фильтруются и до сервиса не доходят. Это разные команды, разные люди и разное время решения.</p>"},
 {h:"На каком адресе слушает сервис",b:"<pre>ss -tulpn | grep 5432\ntcp LISTEN 0 128 127.0.0.1:5432 users:((\"postgres\",pid=820,fd=6))</pre><p>Флаги: <b>t</b> tcp, <b>u</b> udp, <b>l</b> только слушающие, <b>p</b> процесс, <b>n</b> числовые порты без резолвинга имён.</p><p><b>Смотри на адрес слева от порта:</b></p><ul><li><code>127.0.0.1:5432</code> — только локально, снаружи не подключиться. Для базы данных это правильная настройка</li><li><code>0.0.0.0:5432</code> — на всех интерфейсах, доступен из сети</li><li><code>[::]:443</code> — то же самое для IPv6</li></ul><p>Частая ситуация: «порт открыт в фаерволе, а подключиться не могу» — сервис слушает только localhost. Никакие правила фаервола этого не исправят, менять нужно конфигурацию приложения.</p>"},
 {h:"Инструменты, когда простые не помогли",b:"<pre>mtr shop.ru                      <span class='c'># traceroute + статистика потерь</span>\ntcpdump -i eth0 port 443 -nn -c 20  <span class='c'># первые 20 пакетов</span>\ntcpdump -i any host 10.0.1.20 -w dump.pcap\nss -s                            <span class='c'># сводка по соединениям</span>\nss -tn state established | wc -l <span class='c'># сколько активных соединений</span></pre><p><code>mtr</code> позволяет доказать провайдеру, что потери на его участке — с цифрами по каждому узлу, а не «у нас тормозит».</p><p><code>tcpdump</code> — последний рубеж: он показывает, что реально уходит и приходит. Когда логи приложения и прокси противоречат друг другу, пакеты не врут. Флаг <code>-nn</code> отключает резолвинг имён и портов, чтобы вывод не тормозил.</p><p><code>ss -s</code> помогает заметить исчерпание соединений: тысячи в состоянии TIME-WAIT или CLOSE-WAIT — характерный признак проблем с их закрытием в приложении.</p>"},
 {h:"Передача файлов",b:"<pre>scp file.tar.gz user@host:/opt/\nrsync -avz ./dist/ user@host:/var/www/\nrsync -avz --delete --dry-run ./dist/ user@host:/var/www/</pre><p><code>rsync</code> лучше <code>scp</code>: передаёт только изменившиеся части, докачивает после обрыва, сохраняет права и время файлов.</p><p><b>Слеш в конце источника решает всё:</b> <code>dist/</code> копирует <i>содержимое</i> каталога, <code>dist</code> — сам каталог внутрь приёмника. Ошибка даёт <code>/var/www/dist/index.html</code> вместо <code>/var/www/index.html</code> — и это самая частая ошибка с rsync.</p><p><code>--delete</code> удаляет на приёмнике то, чего нет в источнике: превращает копирование в зеркалирование. Мощно и опасно — поэтому сначала <code>--dry-run</code>, всегда.</p>"}],
 ch:[
 {t:"c",d:1,q:"Покажи таблицу маршрутизации современной командой и найди в ней шлюз по умолчанию.",ans:["ip r","ip route","ip route show","ip -4 r"],hint:"две буквы после ip",why:"Строка default указывает шлюз, через который уходит весь трафик наружу. Её отсутствие означает, что интерфейс поднят, локальная сеть работает, а в интернет пакеты не уходят."},
 {t:"c",d:2,q:"Нужно узнать, какие порты слушаются на сервере, какими процессами и на каких адресах.",ans:["ss -tulpn","ss -tulnp","sudo ss -tulpn","netstat -tulpn"],hint:"ss и четыре флага",why:"t — tcp, u — udp, l — слушающие, p — процесс, n — без резолвинга имён. Адрес слева от порта не менее важен, чем сам порт: 127.0.0.1 означает доступ только локально."},
 {t:"q",d:3,q:"Команда <code>nc -zv host 5432</code> отвечает «Connection refused». Что это говорит о проблеме и куда идти дальше?",a:["Пакеты фильтруются фаерволом","Связность есть: пакет дошёл до хоста, но порт никто не слушает — идти к сервису и проверить, запущен ли он и на каком адресе слушает","Проблема с разрешением имени","Маршрут до хоста отсутствует"],c:1,why:"Refused — это активный отказ от хоста, значит сеть работает. Таймаут, наоборот, означает молчание и указывает на фильтрацию. Эта развилка экономит часы, направляя к нужному человеку сразу."},
 {t:"q",d:4,q:"Порт 5432 разрешён в фаерволе, приложение с соседнего сервера не подключается, а <code>ss -tulpn</code> показывает <code>127.0.0.1:5432</code>. В чём причина и какое решение правильное с точки зрения безопасности?",a:["Фаервол всё-таки блокирует, нужно проверить правила ещё раз","Сервис слушает только петлевой интерфейс, и никакие правила фаервола это не изменят. Технически нужно менять адрес привязки в конфиге, но правильнее оставить базу закрытой и ходить к ней по приватной сети или через SSH-туннель","Проблема в разрешении имён","Порт занят другим процессом"],c:1,why:"Здесь важны обе части ответа: понимание, что фаервол ни при чём, и понимание, что «открыть базу наружу» — не то решение, которое стоит принимать по умолчанию."},
 {t:"q",d:4,q:"Чем <code>rsync -avz</code> практически лучше <code>scp</code> при повторной выкатке каталога и какая деталь синтаксиса чаще всего приводит к ошибке?",a:["Ничем существенным, разница в скорости","Передаёт только изменившиеся части, докачивает после обрыва и сохраняет права; главная ловушка — слеш в конце источника: <code>dist/</code> копирует содержимое, а <code>dist</code> — сам каталог внутрь приёмника","rsync шифрует надёжнее","rsync работает без SSH"],c:1,why:"Для больших каталогов разница во времени в разы. А ошибка со слешем даёт /var/www/dist/index.html вместо ожидаемого пути — и обнаруживается уже после выкатки, по 404 на сайте."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Приложение на сервере A перестало обращаться к API на сервере B. <code>ping B</code> проходит без потерь, <code>nc -zv B 443</code> висит и завершается по таймауту. Что это означает и каков план действий?",a:["Упало приложение на сервере B, нужно его перезапустить","Связность на уровне сети есть, но до порта пакеты не доходят: это фильтрация — проверять правила фаервола на B, security group провайдера и маршрут через mtr. Если бы приложение просто упало, ответом был бы Connection refused","Проблема с DNS, нужно проверить резолвинг","Истёк TLS-сертификат на сервере B"],c:1,why:"Ping отвечает за ICMP и говорит только о доступности хоста. Таймаут на конкретном порту при работающем ping — почти всегда правило DROP на фаерволе: оно отбрасывает пакеты молча, в отличие от REJECT, который вернул бы refused."}]}
);
