/* =====================================================================
   МИР 1 «LINUX», углублённая редакция — миссии 1-5
   Теория: 6-8 карточек с механизмом, ошибками и вопросами собеседования
   Задания: развёрнутые формулировки, лесенка d1..d5
   ===================================================================== */
W("linux").miss.length=0;
W("linux").miss.push(

{id:"l1",t:"Первый вход на сервер",goal:"Ориентироваться в чужой системе вслепую",
 cards:[
 {h:"Терминал — это диалог, а не магия",b:"<p>Ты пишешь команду — система отвечает. Никакой скрытой логики: есть программа, есть аргументы, есть результат. Формат почти всегда один:</p><pre><span class='k'>команда</span> -флаги аргументы</pre><p><code>ls -la /etc</code> — программа <code>ls</code>, флаги <code>-l</code> и <code>-a</code> (склеены в один аргумент), путь <code>/etc</code>.</p><p><b>Как устроены флаги.</b> Короткие пишутся одной буквой после дефиса и склеиваются: <code>-la</code> равносильно <code>-l -a</code>. Длинные пишутся через два дефиса и не склеиваются: <code>--all --human-readable</code>. Если флагу нужно значение, оно идёт следующим аргументом (<code>tail -n 50</code>) или через знак равенства у длинной формы (<code>--lines=50</code>).</p><p><b>Почему это важно знать.</b> Когда команда из статьи не работает, в половине случаев дело в том, что флаг требует значение, а его не передали, или два флага склеили, хотя первый ждёт аргумент. <code>tail -fn</code> — не то же самое, что <code>tail -f -n 50</code>.</p><div class='analogy'><b>Аналогия:</b> флаги — это настройки матча перед стартом, аргумент — карта, на которой играешь. Команда одна, поведение разное.</div>"},
 {h:"Три команды, с которых начинается любая смена",b:"<pre>pwd            <span class='c'># где я сейчас (print working directory)</span>\nls -la         <span class='c'># что здесь лежит: l = подробно, a = включая скрытые</span>\ncd /var/log    <span class='c'># перейти в каталог</span></pre><p>Это не «три команды для новичка» — это то, что опытный инженер набирает автоматически в первые пять секунд после подключения к незнакомой машине. Сначала точка отсчёта, потом окружение.</p><div class='tw'><table><tr><th>форма cd</th><th>куда ведёт</th><th>когда пригодится</th></tr><tr><td><code>cd ..</code></td><td>на уровень вверх</td><td>вышел из глубокого пути</td></tr><tr><td><code>cd ~</code> или просто <code>cd</code></td><td>домашний каталог</td><td>вернуться «домой» из любой точки</td></tr><tr><td><code>cd -</code></td><td>предыдущий каталог</td><td>прыгаешь между двумя местами</td></tr><tr><td><code>cd /</code></td><td>корень системы</td><td>смотришь структуру целиком</td></tr></table></div><p><code>cd -</code> недооценён: при разборе инцидента ты постоянно ходишь между <code>/etc/nginx</code> и <code>/var/log/nginx</code>, и один символ экономит десятки нажатий.</p>"},
 {h:"Абсолютный и относительный путь",b:"<p><b>Абсолютный</b> путь начинается со слэша и не зависит от того, где ты стоишь: <code>/var/log/nginx/error.log</code>. Он всегда указывает на одно и то же место.</p><p><b>Относительный</b> отсчитывается от текущего каталога: <code>nginx/error.log</code>, <code>../etc/hosts</code>. Один и тот же текст означает разные файлы в зависимости от того, где ты находишься.</p><ul><li><code>.</code> — текущий каталог</li><li><code>..</code> — родительский</li><li><code>~</code> — домашний каталог текущего пользователя</li><li><code>~deploy</code> — домашний каталог пользователя deploy</li></ul><p><b>Правило, которое спасает продакшены:</b> в командах, которые что-то удаляют, перезаписывают или перемещают, пиши <b>абсолютные пути</b>. И в скриптах тоже — скрипт может быть запущен из любого каталога, в том числе из cron с непредсказуемым рабочим каталогом.</p><p>Относительный путь плюс неверное представление о том, где ты стоишь, — самый дешёвый способ удалить чужие данные. Команда при этом отработает без единой ошибки.</p>"},
 {h:"Карта чужой системы",b:"<p>Файловая система Linux устроена по соглашению (стандарт FHS), поэтому на любом сервере ты знаешь, куда идти, ещё не открыв его.</p><div class='tw'><table><tr><th>каталог</th><th>что там</th><th>зачем тебе</th></tr><tr><td><code>/etc</code></td><td>конфигурации всех сервисов</td><td>первое место при настройке</td></tr><tr><td><code>/var/log</code></td><td>логи</td><td>первое место при аварии</td></tr><tr><td><code>/var/lib</code></td><td>данные сервисов: базы, docker, пакеты</td><td>ищешь, что занимает диск</td></tr><tr><td><code>/home/имя</code></td><td>файлы пользователей</td><td>ключи, скрипты, история</td></tr><tr><td><code>/root</code></td><td>дом суперпользователя — <b>не</b> в /home</td><td>частый сюрприз</td></tr><tr><td><code>/opt</code></td><td>стороннее и собственное ПО</td><td>тут обычно живёт приложение</td></tr><tr><td><code>/tmp</code></td><td>временные файлы, чистятся при перезагрузке</td><td>сюда нельзя класть важное</td></tr><tr><td><code>/usr/bin</code>, <code>/usr/local/bin</code></td><td>программы системы и установленные вручную</td><td>ищешь, откуда запускается бинарник</td></tr><tr><td><code>/proc</code>, <code>/sys</code></td><td>виртуальные ФС ядра</td><td>живое состояние системы</td></tr></table></div><p><code>/proc</code> — не каталог с файлами, а интерфейс к ядру: <code>cat /proc/cpuinfo</code> покажет процессор, <code>cat /proc/1234/environ</code> — переменные окружения процесса 1234. Файлов на диске при этом не существует, они генерируются в момент чтения.</p>"},
 {h:"Как читать вывод ls -la построчно",b:"<pre>drwxr-xr-x  2 root  root   4096 фев 12 09:14 nginx\n-rw-r--r--  1 app   app   84213 фев 12 09:41 app.log\nlrwxrwxrwx  1 root  root     23 фев 12 09:00 current -&gt; releases/v2\n│└──┬──┘    │  │     │      │        │          │\n│   │       │  │     │      │        │          └ имя (для ссылки — и цель)\n│   │       │  │     │      │        └ дата последнего изменения\n│   │       │  │     │      └ размер в байтах\n│   │       │  │     └ группа-владелец\n│   │       │  └ пользователь-владелец\n│   │       └ число жёстких ссылок на данные\n│   └ права: три группы по три символа\n└ тип файла</pre><p><b>Первый символ — самый информативный:</b></p><ul><li><code>-</code> обычный файл</li><li><code>d</code> каталог</li><li><code>l</code> символическая ссылка (настоящий файл в другом месте!)</li><li><code>b</code> / <code>c</code> блочное и символьное устройство</li><li><code>s</code> сокет — так общаются процессы, например nginx с приложением</li></ul><p>Увидел <code>l</code> — прежде чем править файл, посмотри, куда ведёт ссылка. Иначе рискуешь отредактировать не то, что думаешь, или сломать схему деплоя, где <code>current</code> переключается между релизами.</p>"},
 {h:"Когда не знаешь команду",b:"<pre>man ls          <span class='c'># полное руководство: q выход, /слово поиск, n следующее</span>\nls --help       <span class='c'># краткая справка, работает почти везде</span>\nwhich nginx     <span class='c'># путь к исполняемому файлу</span>\ntype cd         <span class='c'># что это: программа, встроенная команда, алиас</span>\napropos network <span class='c'># поиск команд по описанию</span></pre><p><code>--help</code> обычно быстрее <code>man</code> и, что важнее, <b>работает в контейнерах</b>, где man-страницы не устанавливают ради экономии размера образа.</p><p><code>type</code> отвечает на вопрос, который часто ставит в тупик: почему <code>cd</code> нет в <code>/usr/bin</code>? Потому что это встроенная команда оболочки, а не программа — она обязана менять состояние самой оболочки, отдельный процесс этого сделать не смог бы.</p>"},
 {h:"Частые ошибки первого дня",b:"<ul><li><b>Пробелы в именах.</b> <code>cd /opt/my app</code> — это два аргумента. Нужно <code>cd \"/opt/my app\"</code> или <code>cd /opt/my\\ app</code>.</li><li><b>Регистр имеет значение.</b> <code>App.log</code> и <code>app.log</code> — разные файлы, в отличие от Windows.</li><li><b>Точка в начале — скрытый файл.</b> <code>.env</code>, <code>.git</code>, <code>.ssh</code> не видны без <code>-a</code>, и именно в них лежит самое интересное.</li><li><b>Автодополнение по Tab.</b> Не роскошь, а защита от опечаток в путях: если Tab не дополняет, значит такого пути нет — и ты узнаёшь об этом до выполнения команды.</li><li><b>Стрелка вверх</b> и <code>Ctrl+R</code> (поиск по истории) экономят больше времени, чем любая другая привычка в терминале.</li></ul>"}],
 ch:[
 {t:"c",d:1,q:"Ты подключился по SSH к незнакомому серверу и не понимаешь, в каком каталоге оказался. Спроси об этом систему.",ans:["pwd"],hint:"print working directory",why:"pwd — компас и первая команда на чужой машине. Всё остальное имеет смысл только после того, как ты знаешь точку отсчёта."},
 {t:"c",d:2,q:"Тебе сказали: «конфиг лежит в домашнем каталоге, но я его не вижу». Покажи полное содержимое текущего каталога — подробно и включая скрытые файлы.",ans:["ls -la","ls -al","ls -a -l","ls -l -a"],hint:"два флага: длинный формат и all",why:"Скрытые файлы начинаются с точки: .env, .git, .ssh, .bashrc. Без -a ты их не увидишь и будешь искать «пропавший» конфиг, который лежит на самом видном месте."},
 {t:"q",d:2,q:"В выводе <code>ls -la</code> строка начинается с символа <code>l</code>, а в конце имени стоит стрелка: <code>current -> releases/v2</code>. Что это и что важно помнить, прежде чем править такой файл?",a:["Обычный файл с длинным именем","Символическая ссылка: настоящий файл лежит по другому пути, и правки уйдут туда","Каталог с особыми правами","Файл, заблокированный другим процессом"],c:1,why:"Первый символ строки — тип файла. Символические ссылки лежат в основе схемы деплоя: current указывает на текущий релиз, и переключение ссылки равносильно выкатке или откату."},
 {t:"q",d:3,q:"Ты находишься в <code>/var/log/nginx</code> и хочешь посмотреть <code>/etc/hosts</code>. Коллега предлагает набрать <code>../../../etc/hosts</code>. Что скажешь и почему?",a:["Так короче, пусть будет","Лучше абсолютный путь <code>/etc/hosts</code>: он не зависит от текущего каталога, а значит команду можно повторить откуда угодно и вставить в скрипт","Разницы нет","Относительный путь работает быстрее"],c:1,why:"Относительный путь требует держать в голове, где ты стоишь. В интерактивной работе это лишняя нагрузка, а в скрипте — прямая ошибка: рабочий каталог при запуске может быть любым."},
 {t:"o",d:4,q:"Тебя пустили на незнакомый сервер со словами «разберись, что тут вообще происходит». Расставь первые шаги в том порядке, в котором они дают максимум информации при минимуме действий.",items:["pwd — понять точку отсчёта","ls -la — увидеть содержимое, включая скрытое","cd /etc и ls — узнать, какие сервисы настроены","cd /var/log и ls -la — найти их логи","tail свежего лога — понять, что происходит прямо сейчас"],why:"Логика: где я → что вокруг → что настроено → где следы работы → что происходит сейчас. Обратный порядок заставляет читать логи сервисов, о существовании которых ты ещё не знаешь."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Коллега просит: «зайди на сервер и почисти старые файлы в каталоге релизов». Ты подключаешься, набираешь <code>rm -rf releases/old</code> и получаешь «No such file or directory». Коллега уверяет, что каталог точно есть. Какое действие правильное и какой вывод стоит сделать из самого факта ошибки?",a:["Повторить команду с sudo — вероятно, не хватило прав","Проверить <code>pwd</code> и убедиться, что ты в нужном каталоге: относительный путь считается от текущего места, и ошибка здесь — удача, потому что та же команда из другого каталога могла удалить чужие данные молча","Добавить флаг -f, чтобы подавить сообщение","Перезапустить SSH-сессию"],c:1,why:"Ошибка «нет такого файла» при удалении — подарок: она означает, что ничего не удалено. Опасен обратный случай, когда относительный путь случайно совпал с существующим. Отсюда правило: удаляющие команды пишутся абсолютным путём и предваряются ls по тому же пути."}]},

{id:"l2",t:"Чтение логов",goal:"Найти нужное в сотнях мегабайт текста",
 cards:[
 {h:"Файл не обязательно открывать целиком",b:"<pre>cat file.log       <span class='c'># вывалить весь файл в терминал</span>\nless file.log      <span class='c'># листать: q выход, /слово поиск, G в конец, g в начало</span>\nhead -20 file.log  <span class='c'># первые 20 строк</span>\ntail -50 file.log  <span class='c'># последние 50 строк</span>\ntail -f app.log    <span class='c'># следить за новыми строками в реальном времени</span></pre><p><code>cat</code> на файле в гигабайт заблокирует терминал на минуты и вытеснит из буфера прокрутки всё полезное. Для больших файлов существуют <code>less</code>, <code>head</code> и <code>tail</code>.</p><p><b>Почему начинают с конца.</b> Логи пишутся по возрастанию времени, поэтому свежие события — внизу. При разборе инцидента, который произошёл только что, <code>tail</code> даёт нужное сразу, а <code>head</code> покажет, что было при запуске сервиса неделю назад.</p><p><code>tail -f</code> — самая используемая команда дежурного: смотришь лог живьём, пока коллега воспроизводит проблему. <code>tail -F</code> надёжнее: он переживает ротацию, когда файл переименовали и создали новый, — обычный <code>-f</code> в этот момент замолкает навсегда, продолжая держать старый файл.</p>"},
 {h:"grep: поиск по содержимому",b:"<div class='tw'><table><tr><th>флаг</th><th>что делает</th><th>когда нужен</th></tr><tr><td><code>-i</code></td><td>без учёта регистра</td><td>ERROR, Error, error в одном логе</td></tr><tr><td><code>-c</code></td><td>сколько строк совпало</td><td>оценить масштаб</td></tr><tr><td><code>-n</code></td><td>номера строк</td><td>чтобы потом открыть в редакторе</td></tr><tr><td><code>-v</code></td><td>строки БЕЗ совпадения</td><td>убрать шум</td></tr><tr><td><code>-r</code></td><td>рекурсивно по каталогу</td><td>не знаешь, в каком файле</td></tr><tr><td><code>-A5</code> <code>-B5</code> <code>-C5</code></td><td>строки после / до / вокруг</td><td>нужен контекст</td></tr><tr><td><code>-E</code></td><td>расширенные регулярные выражения</td><td>несколько вариантов сразу</td></tr></table></div><pre>grep -c ERROR app.log\ngrep -C5 \"connection refused\" app.log\ngrep -rn timeout /etc/nginx/\ngrep -E \"ERROR|FATAL|panic\" app.log</pre><p><b>Тонкость с <code>-c</code>:</b> он считает <i>строки</i>, а не совпадения. Если в одной строке слово встречается трижды, это всё равно единица.</p>"},
 {h:"Кавычки в grep — не украшение",b:"<p><code>grep connection refused app.log</code> ищет слово <code>connection</code> в файлах <code>refused</code> и <code>app.log</code>. Первый аргумент — шаблон, все остальные — файлы.</p><p>Правильно: <code>grep \"connection refused\" app.log</code>.</p><p>Кавычки нужны всегда, когда в шаблоне есть пробелы или символы, которые оболочка попытается истолковать: <code>*</code>, <code>?</code>, <code>$</code>, <code>(</code>. Одинарные кавычки жёстче двойных: внутри них оболочка не подставляет переменные вообще.</p><p><b>Как это выглядит в жизни:</b> команда отрабатывает без ошибки, выводит что-то неожиданное или ничего, и человек делает вывод «в логах пусто». На самом деле искали не то и не там.</p>"},
 {h:"Контекст важнее самой ошибки",b:"<p>Строка <code>ERROR: connection refused</code> сама по себе не говорит почти ничего. Ценность — в том, что было <b>до</b> неё (какой запрос обрабатывался, к чему подключались) и <b>после</b> (приложение упало, повторило попытку или продолжило работу).</p><p>Поэтому в разборе инцидента <code>grep -C5</code> полезнее голого <code>grep</code>: ты сразу видишь сцену целиком, а не отдельный крик.</p><pre>grep -B10 -A5 \"FATAL\" app.log</pre><p>Здесь десять строк до — потому что причина обычно предшествует фатальной ошибке, а после неё процесс уже завершается.</p>"},
 {h:"Пайп: конвейер из простых команд",b:"<p>Символ <code>|</code> передаёт вывод одной команды на вход другой. Философия Unix: много маленьких инструментов, каждый делает одно дело, а сложные ответы собираются их соединением.</p><pre>cat access.log | grep \" 500 \" | wc -l\n<span class='c'># сколько всего было пятисоток</span>\n\ngrep ERROR app.log | tail -20\n<span class='c'># двадцать последних ошибок</span>\n\ngrep ERROR app.log | grep -v \"timeout\" | head\n<span class='c'># ошибки, кроме таймаутов</span></pre><p><b>Полезное упрощение:</b> <code>grep ERROR app.log | wc -l</code> и <code>grep -c ERROR app.log</code> дают одно и то же, но второй вариант короче и не порождает лишний процесс. Такие мелочи не важны на одном файле и важны в скрипте, который обрабатывает тысячи.</p><div class='analogy'><b>Аналогия:</b> это крафт. Каждая команда — простой ингредиент, пайп — верстак, на котором из них собирается нужный предмет.</div>"},
 {h:"Сузить по времени — первый шаг, а не последний",b:"<p>В инциденте важен не весь лог, а окно вокруг события. Порядок «сначала окно, потом фильтр» экономит и время, и ресурсы сервера:</p><pre>grep \"09:4\" app.log | grep ERROR      <span class='c'># всё за 09:40–09:49, затем ошибки</span>\ntail -5000 app.log | grep ERROR       <span class='c'># свежий хвост, затем ошибки</span>\nsed -n '/09:40/,/09:50/p' app.log     <span class='c'># строго диапазон строк между метками</span></pre><p>Обратный порядок (<code>grep ERROR</code> по всему файлу, потом отбор по времени) заставляет систему прочитать гигабайты ради строк, которые заведомо вне интересующего периода. На файле в 4 ГБ разница ощутима.</p><p>Для журналов systemd есть готовые фильтры: <code>journalctl -u api --since \"09:40\" --until \"09:50\"</code>.</p>"},
 {h:"Где искать, если логов нет там, где ждёшь",b:"<div class='tw'><table><tr><th>где</th><th>что там</th></tr><tr><td><code>/var/log/syslog</code> или <code>messages</code></td><td>общий системный журнал</td></tr><tr><td><code>/var/log/nginx/{access,error}.log</code></td><td>веб-сервер: запросы и ошибки</td></tr><tr><td><code>journalctl -u имя</code></td><td>журнал systemd-сервиса</td></tr><tr><td><code>docker logs имя</code></td><td>stdout контейнера</td></tr><tr><td><code>/var/log/auth.log</code></td><td>входы, sudo, SSH — при разборе инцидентов безопасности</td></tr></table></div><p>В контейнерах приложение обычно пишет в stdout, а не в файл, — искать файлы внутри контейнера бесполезно. Это не случайность, а принцип: доставкой логов занимается инфраструктура, а не приложение.</p>"},
 {h:"Как это спрашивают на собеседовании",b:"<p>Формулировка обычно такая: «в логе на несколько гигабайт нужно найти причину ошибки, случившейся в 09:41. Твои действия?»</p><p>Слабый ответ — назвать <code>grep</code>. Сильный — показать метод:</p><ol><li>сузить окно по времени</li><li>найти ошибки внутри окна</li><li>взять контекст вокруг найденного</li><li>сопоставить с логами соседнего компонента (веб-сервер и приложение) по времени</li></ol><p>И упомянуть, что <code>cat огромный_файл</code> — плохая идея. Проверяют не знание флагов, а понимание, что делать с объёмом.</p>"}],
 ch:[
 {t:"c",d:1,q:"Сервис только что перезапустился, и нужно быстро увидеть, что он написал последним. Покажи последние 50 строк файла /var/log/syslog.",ans:["tail -n 50 /var/log/syslog","tail -50 /var/log/syslog"],hint:"tail -n число файл",why:"Чтение лога начинают с конца: свежие события внизу. head покажет то, что происходило при первом запуске сервиса недели назад."},
 {t:"c",d:2,q:"Тебя спрашивают, насколько всё плохо. Посчитай, в скольких строках файла app.log встречается слово ERROR.",ans:["grep -c error app.log","grep -c ERROR app.log","grep -ci error app.log","grep -c \"ERROR\" app.log"],hint:"у grep есть флаг count",why:"grep -c считает строки с совпадением, а не сами совпадения. Эквивалент через пайп — grep ERROR app.log | wc -l, но короткая форма не порождает лишний процесс."},
 {t:"c",d:3,q:"Нужно понять, что происходило вокруг каждой ошибки подключения: покажи по 5 строк до и после каждого вхождения фразы «connection refused» в файле app.log.",ans:["grep -c5 connection refused app.log","grep -C5 \"connection refused\" app.log","grep -C 5 \"connection refused\" app.log","grep -c 5 connection refused app.log"],hint:"флаг контекста и обязательные кавычки вокруг фразы из двух слов",why:"Без кавычек grep примет «refused» за имя файла и будет искать слово «connection» не там. -C5 равносильно -A5 -B5: сцена целиком вместо вырванной строки."},
 {t:"q",d:4,q:"Лог приложения весит 4 ГБ, инцидент был в 09:41. Коллега предлагает выполнить <code>cat app.log | grep ERROR | grep \"09:41\"</code>. Что не так с этим подходом и как сделать правильно?",a:["Ничего, команда рабочая","Порядок неэффективен: сначала читается весь файл и фильтруются все ошибки за всё время. Правильно сначала сузить окно (<code>grep \"09:4\" app.log</code> или <code>tail -5000</code>), а внутри него искать ошибки","Нужно заменить cat на less","grep нельзя вызывать дважды"],c:1,why:"Результат получится тот же, но ценой чтения гигабайтов. Плюс лишний cat: grep умеет читать файл сам. Правило простое — сначала сузить область, потом фильтровать."},
 {t:"q",d:4,q:"Лог забит строками INFO, среди которых теряется всё остальное. Ты не знаешь заранее, как выглядят интересующие тебя сообщения. Какой приём применить?",a:["Искать по слову ERROR и надеяться","Инвертировать условие: <code>grep -v INFO app.log | tail -100</code> — убрать заведомо неинтересное вместо угадывания искомого","Открыть файл в редакторе и листать","Увеличить размер терминала"],c:1,why:"Приём «убрать шум» работает, когда не знаешь точной формулировки ошибки. Часто уровни называются по-разному (WARN, WARNING, warn), и поиск по конкретному слову пропускает половину."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Клиенты жалуются на ошибки при оформлении заказа прямо сейчас. Ты открыл <code>tail -f /var/log/app/app.log</code> и видишь ровный поток INFO без единой ошибки. Коллега в этот момент воспроизводит проблему в браузере и подтверждает: ошибка на экране есть. Что происходит и что делать дальше?",a:["Логирование отключено, нужно включить debug","Ты смотришь только один компонент цепочки: ошибка может возникать в nginx, в другом сервисе или до того, как запрос дошёл до приложения — нужно параллельно смотреть error.log веб-сервера и сопоставлять по времени","Приложение зависло и не пишет логи","Файл лога переполнен"],c:1,why:"Запрос проходит через несколько компонентов, и каждый ведёт свой журнал. Тактика при «ошибка есть, а в логах чисто»: смотреть с обоих концов цепочки — веб-сервер и приложение — и сверять по времени. Часто выясняется, что запрос до приложения вообще не доходил."}]},

{id:"l3",t:"Права доступа",goal:"Понять, почему Permission denied — и починить правильно",
 cards:[
 {h:"Три роли и три права",b:"<pre>-rw-r--r--  1 deploy www-data  app.conf\n ^^^ ^^^ ^^^\n  │   │   └── остальные (others): r--\n  │   └────── группа (group):     r--\n  └────────── владелец (user):    rw-</pre><p>Права: <b>r</b> чтение, <b>w</b> запись, <b>x</b> выполнение. Каждое из них задаётся отдельно для трёх категорий.</p><p><b>Как система принимает решение.</b> Проверка идёт по порядку и останавливается на первом совпадении: ты владелец файла? применяются права владельца, дальше не смотрим. Нет — состоишь в группе-владельце? права группы. Нет — права остальных.</p><p><b>Неочевидное следствие:</b> если владельцу запрещено, а остальным разрешено, владелец всё равно не получит доступ. Права <code>----rw-rw-</code> означают, что владелец — единственный, кто не может читать собственный файл. Выглядит абсурдно, но именно так работает проверка.</p>"},
 {h:"Цифры: r=4, w=2, x=1",b:"<p>Складываешь нужные права и получаешь цифру для каждой из трёх категорий.</p><div class='tw'><table><tr><th>цифра</th><th>права</th><th>типичное применение</th></tr><tr><td>7</td><td>rwx</td><td>каталоги, куда нужно писать</td></tr><tr><td>6</td><td>rw-</td><td>обычные файлы владельца</td></tr><tr><td>5</td><td>r-x</td><td>чтение файла и вход в каталог</td></tr><tr><td>4</td><td>r--</td><td>только чтение</td></tr><tr><td>0</td><td>---</td><td>полный запрет</td></tr></table></div><pre>chmod 644 app.conf   <span class='c'># владелец пишет, остальные читают</span>\nchmod 755 script.sh  <span class='c'># исполняемый скрипт</span>\nchmod 600 id_rsa     <span class='c'># только владелец: иначе ssh откажется работать</span>\nchmod 700 ~/.ssh     <span class='c'># каталог с ключами</span>\nchmod u+x deploy.sh  <span class='c'># буквенная форма: добавить x владельцу</span></pre><p>Буквенная форма (<code>u</code> user, <code>g</code> group, <code>o</code> others, <code>a</code> all; <code>+</code> добавить, <code>-</code> убрать) удобна, когда нужно изменить одно право, не трогая остальные. Цифровая — когда задаёшь права целиком.</p>"},
 {h:"Права на каталог означают другое",b:"<div class='tw'><table><tr><th>право</th><th>на файле</th><th>на каталоге</th></tr><tr><td><b>r</b></td><td>прочитать содержимое</td><td>получить список имён</td></tr><tr><td><b>w</b></td><td>изменить содержимое</td><td>создавать и <b>удалять</b> файлы внутри</td></tr><tr><td><b>x</b></td><td>запустить</td><td>войти и обратиться к файлу по имени</td></tr></table></div><p>Отсюда два неочевидных следствия, которые регулярно ставят людей в тупик.</p><p><b>Первое:</b> чтобы прочитать <code>/opt/app/config.yml</code>, нужен бит <b>x</b> на всех каталогах пути — <code>/opt</code> и <code>/opt/app</code> — и бит <b>r</b> на самом файле. Права на файл могут быть идеальными, а доступа не будет.</p><p><b>Второе:</b> удаление файла — это изменение <i>каталога</i>, а не файла. Если у тебя есть <b>w</b> на каталог, ты удалишь файл, даже если он принадлежит другому пользователю и закрыт правами <code>600</code>. Именно поэтому у <code>/tmp</code> есть специальная защита (sticky bit).</p>"},
 {h:"Владелец и группа",b:"<pre>chown deploy:deploy app.conf     <span class='c'># сменить владельца и группу</span>\nchown -R app:app /opt/app        <span class='c'># рекурсивно по каталогу</span>\nchgrp www-data app.conf          <span class='c'># только группу</span>\nid deploy                        <span class='c'># uid, gid и все группы пользователя</span>\ngroups deploy</pre><p>Типичная схема на веб-сервере: файлы принадлежат пользователю <code>deploy</code>, группа — <code>www-data</code>, права <code>640</code>. Деплой пишет, nginx (работающий от www-data) читает, посторонние не видят ничего.</p><p><b>Важно:</b> внутри системы права хранятся не по именам, а по числовым идентификаторам — <b>UID</b> и <b>GID</b>. Имена — просто удобная подстановка из <code>/etc/passwd</code>. Поэтому при монтировании томов в контейнеры или переносе дисков между машинами файлы «меняют владельца»: UID тот же, а какому имени он соответствует — зависит от системы.</p>"},
 {h:"umask: откуда берутся права новых файлов",b:"<p>Когда программа создаёт файл, она запрашивает права <code>666</code> (для каталогов <code>777</code>), а система вычитает из них <b>umask</b>.</p><pre>umask            <span class='c'># обычно 0022</span>\n666 - 022 = 644  <span class='c'># права нового файла</span>\n777 - 022 = 755  <span class='c'># права нового каталога</span></pre><p>Отсюда ответ на вопрос «почему созданные файлы всегда 644, я же ничего не настраивал». И отсюда же способ изменить поведение для сервиса: <code>UMask=0027</code> в systemd-юните сделает файлы недоступными для чтения посторонним.</p>"},
 {h:"Как чинить Permission denied правильно",b:"<p>Порядок мысли всегда один: <b>кто</b> обращается → <b>к чему</b> → <b>какого именно права не хватает</b>.</p><pre>ls -la /opt/app/config.yml   <span class='c'># чей файл и с какими правами</span>\nid app                       <span class='c'># от кого работает сервис и в каких он группах</span>\nnamei -l /opt/app/config.yml <span class='c'># права на каждом шаге пути</span>\nsudo -u app cat /opt/app/config.yml  <span class='c'># воспроизвести от имени сервиса</span></pre><p>Последняя команда — самая честная проверка: ты не рассуждаешь, а буквально повторяешь то, что делает сервис.</p><p><b>Никогда не лечи это через <code>chmod 777</code>.</b> Это открывает файл на запись всем пользователям системы и превращает локальную неурядицу в дыру в безопасности. Правильный ответ почти всегда один из двух: сменить владельца или группу (<code>chown</code>) либо добавить сервисного пользователя в нужную группу.</p><p>На собеседовании ответ «поставил 777, заработало» закрывает вакансию быстрее, чем незнание команды.</p>"}],
 ch:[
 {t:"q",d:1,q:"Ты выполнил <code>chmod 600 id_rsa</code> для приватного SSH-ключа. Что означают эти права и почему именно они нужны для ключа?",a:["Читать и писать может только владелец; SSH отказывается использовать ключ с более широкими правами","Все пользователи могут читать файл","Файл стал исполняемым","Файл получил нового владельца"],c:0,why:"6 = rw для владельца, 0 и 0 — ничего группе и остальным. Клиент SSH проверяет права и отказывается работать с ключом, который могут прочитать другие пользователи машины: это защита от кражи ключа соседом по серверу."},
 {t:"c",d:2,q:"Скрипт deploy.sh нужно сделать исполняемым: владелец получает полный доступ, все остальные — только чтение и выполнение. Задай права цифрой.",ans:["chmod 755 deploy.sh","sudo chmod 755 deploy.sh"],hint:"rwx для владельца и r-x для остальных — сложи r=4, w=2, x=1",why:"7 = rwx, 5 = r-x. Это стандартные права исполняемого файла. Вариант 777 добавил бы всем право записи — то есть возможность подменить содержимое скрипта, который потом кто-то запустит от root."},
 {t:"q",d:3,q:"Файл <code>/opt/app/secret.yml</code> имеет права 644 и принадлежит пользователю app. Сервис работает от пользователя app, но получает Permission denied при чтении. Права на сам файл верные. Что проверить и какой командой?",a:["Права ещё раз, возможно, опечатка","Права на каталоги пути: для обращения к файлу нужен бит x на /opt и /opt/app — команда <code>namei -l /opt/app/secret.yml</code> покажет весь путь целиком","Свободное место на диске","Версию ядра"],c:1,why:"Доступ к файлу требует прохода по всем каталогам пути, а для этого нужен бит x на каждом. namei -l показывает права по всей цепочке — экономит время, которое иначе уходит на проверку каталогов по одному."},
 {t:"q",d:4,q:"Коллега «починил» доступ командой <code>chmod -R 777 /var/www</code>: сайт заработал. Объясни, что теперь не так и как следовало поступить.",a:["Всё в порядке, главное — результат","Любой пользователь системы, включая скомпрометированный процесс, может изменить файлы сайта — это дыра в безопасности; правильно сменить владельца или группу (chown deploy:www-data) и выставить 640/750","Нужно было использовать 666","Не хватало флага -f"],c:1,why:"777 — классическая находка аудита. Задача «дать доступ конкретному сервису» решается принадлежностью к группе, а не раздачей прав всему миру. Плюс рекурсивный chmod по каталогу ломает биты x у подкаталогов, если применён неаккуратно."},
 {t:"o",d:4,q:"Сервис не может прочитать свой конфигурационный файл. Расставь шаги диагностики так, чтобы каждое следующее действие опиралось на факты, полученные предыдущим.",items:["ls -la на файл — узнать владельца, группу и права","id имя_сервисного_пользователя — понять, кто именно обращается и в каких он группах","namei -l путь — проверить права на всех каталогах пути","sudo -u пользователь cat файл — воспроизвести проблему от его имени","chown или добавление в группу — точечно выдать недостающее право"],why:"Сначала факты, потом минимальное вмешательство. Изменение прав без этих шагов — угадывание, которое обычно заканчивается расширением доступа больше необходимого."},
 {t:"q",d:5,q:"<b>Инцидент.</b> После деплоя сайт отдаёт 403. В логе nginx: <code>open() \"/var/www/app/index.html\" failed (13: Permission denied)</code>. Проверка показывает: файлы принадлежат <code>deploy:deploy</code> с правами <code>640</code>, а nginx работает от пользователя <code>www-data</code>. Какое изменение решает задачу минимальным способом?",a:["chmod -R 777 на весь каталог сайта","Сменить группу файлов на www-data: <code>chown -R deploy:www-data /var/www/app</code> — права 640 дадут группе чтение, и nginx получит доступ, оставаясь без прав записи","Запустить nginx от имени root","Скопировать файлы заново от имени www-data"],c:1,why:"640 означает «чтение для группы». Достаточно сделать группой-владельцем ту, от имени которой работает веб-сервер. Это минимальное изменение, закрывающее ровно проблему: nginx читает, но не может изменить файлы сайта — что и требуется."}]},

{id:"l4",t:"Процессы и ресурсы",goal:"Узнать, кто съел процессор, память и диск",
 cards:[
 {h:"Что такое процесс",b:"<p>Процесс — запущенная программа со своим номером (<b>PID</b>), владельцем, потреблением ресурсов и родителем. Все процессы образуют дерево, растущее от PID 1 (<code>systemd</code>), который усыновляет осиротевших.</p><pre>ps aux                   <span class='c'># все процессы, подробно</span>\nps aux | grep nginx      <span class='c'># найти по имени</span>\nps -ef --forest          <span class='c'># дерево родитель-потомок</span>\npgrep -a nginx           <span class='c'># короче: PID и команда</span>\ntop / htop               <span class='c'># живой монитор</span></pre><p>В выводе <code>ps aux</code> колонки: пользователь, PID, %CPU, %MEM, VSZ, RSS, TTY, состояние, время старта, команда.</p><p><b>Самая полезная колонка — последняя.</b> Полная команда с аргументами обычно и объясняет, что это за процесс: <code>python3 /opt/tmp/debug_server.py 8080</code> рассказывает о себе всё, включая то, что кто-то забыл выключить отладочный сервер.</p>"},
 {h:"Память процесса: VSZ и RSS",b:"<div class='tw'><table><tr><th>колонка</th><th>что означает</th></tr><tr><td><b>VSZ</b></td><td>виртуальный размер: всё адресное пространство, включая то, что никогда не будет загружено</td></tr><tr><td><b>RSS</b></td><td>резидентная память: сколько реально занято в оперативной памяти</td></tr></table></div><p>Смотреть надо на <b>RSS</b>. VSZ у процесса легко бывает в десятки гигабайт при реальном потреблении в 200 МБ — это нормально и не означает утечку.</p><p>Отдельная тонкость: сумма RSS всех процессов обычно больше, чем занятая память системы, потому что общие библиотеки учитываются у каждого процесса отдельно. Для точной оценки одного приложения есть <code>smem</code> и метрика PSS.</p>"},
 {h:"Сигналы: как правильно останавливать",b:"<div class='tw'><table><tr><th>сигнал</th><th>номер</th><th>смысл</th><th>можно перехватить</th></tr><tr><td>SIGTERM</td><td>15</td><td>«заверши работу» — по умолчанию у kill</td><td>да</td></tr><tr><td>SIGKILL</td><td>9</td><td>убить немедленно</td><td><b>нет</b></td></tr><tr><td>SIGHUP</td><td>1</td><td>у демонов обычно «перечитай конфиг»</td><td>да</td></tr><tr><td>SIGINT</td><td>2</td><td>то же, что Ctrl+C</td><td>да</td></tr><tr><td>SIGSTOP</td><td>19</td><td>приостановить</td><td>нет</td></tr></table></div><pre>kill 4210      <span class='c'># SIGTERM, вежливо</span>\nkill -9 4210   <span class='c'># SIGKILL, последнее средство</span>\nkill -1 711    <span class='c'># перечитать конфигурацию</span></pre><p><b>Почему порядок важен.</b> Получив SIGTERM, приложение успевает сбросить буферы на диск, закрыть соединения с базой и завершить текущие запросы. SIGKILL перехватить невозможно: процесс исчезает мгновенно, оставляя недописанные файлы и оборванные транзакции.</p><p>Правило: сначала обычный <code>kill</code>, подождать 5–10 секунд, и только потом <code>-9</code>. Именно так устроен и <code>docker stop</code>, и остановка пода в Kubernetes.</p>"},
 {h:"Память системы: почему free почти всегда мал",b:"<pre>free -h\n              total    used    free   buff/cache   available\nMem:           3.8Gi   1.2Gi   0.4Gi        2.2Gi       2.4Gi</pre><p><b>Главная ловушка новичка:</b> малое значение <code>free</code> — это норма, а не проблема.</p><p>Linux использует свободную память под дисковый кэш (<code>buff/cache</code>): однажды прочитанные файлы остаются в памяти, чтобы следующее чтение было мгновенным. Как только приложению понадобится память, кэш освободится автоматически.</p><p>Смотреть надо на колонку <b>available</b> — сколько реально доступно программам с учётом освобождаемого кэша. Тревога начинается, когда available близок к нулю и растёт использование swap: система начинает выгружать страницы на диск, и всё замедляется на порядки.</p><p>Соответственно, «свободной памяти мало, надо докупить» — вывод из неправильной колонки. Это классический вопрос на собеседовании.</p>"},
 {h:"Диск: df против du",b:"<pre>df -h              <span class='c'># сколько занято на разделах</span>\ndf -i              <span class='c'># сколько занято inode</span>\ndu -sh /var/*      <span class='c'># сколько весят каталоги</span>\ndu -sh /var/* | sort -h | tail   <span class='c'># самые тяжёлые в конце</span></pre><p>Эти команды считают по-разному: <code>df</code> спрашивает файловую систему, <code>du</code> обходит файлы и складывает их размеры. Расхождение — само по себе диагноз.</p><p><b>Случай первый:</b> <code>df</code> показывает переполнение, <code>du</code> не находит объём. Значит, процесс держит открытым уже удалённый файл: ядро не освобождает блоки, пока жив последний файловый дескриптор. Ищется через <code>lsof | grep deleted</code>, лечится перезапуском этого процесса.</p><p><b>Случай второй:</b> места вроде бы полно, но система пишет «No space left on device». Смотри <code>df -i</code>: закончились inode. Их количество фиксируется при создании файловой системы, и миллионы мелких файлов исчерпывают лимит раньше, чем байты.</p>"},
 {h:"Нагрузка: как читать load average",b:"<pre>uptime\n load average: 2.31, 1.84, 1.02\n                 1м    5м    15м</pre><p>Load average — среднее число процессов, которые выполняются <b>или ждут</b> (в том числе ввода-вывода). Это не проценты и не «загрузка процессора».</p><p><b>Сравнивать надо с числом ядер:</b> LA 4.0 на четырёх ядрах — полная загрузка, на одном — четырёхкратная перегрузка. Узнать число ядер: <code>nproc</code>.</p><p><b>Как читать три числа.</b> Первое больше остальных — нагрузка растёт прямо сейчас. Меньше — пик уже прошёл. Это позволяет за одну команду понять, идёт ли инцидент по нарастающей.</p><p><b>Высокий LA при низком %CPU</b> означает ожидание: медленный диск, зависшее сетевое хранилище, исчерпанный пул соединений. В <code>top</code> это видно по показателю <code>wa</code> (iowait).</p>"},
 {h:"Порядок диагностики «сервер тормозит»",b:"<ol><li><code>uptime</code> — есть ли вообще перегрузка и растёт ли она</li><li><code>top</code> — кто потребляет CPU и память, велик ли iowait</li><li><code>free -h</code> — смотрим available, не free; есть ли swap</li><li><code>df -h</code> и <code>df -i</code> — не переполнен ли диск</li><li><code>ss -tulpn</code> — что слушает, не исчерпаны ли соединения</li><li>логи конкретного подозреваемого сервиса</li></ol><p>Смысл порядка: сначала дешёвые команды, дающие общую картину, потом узкие. Так за минуту ты сужаешь пространство поиска с «что-то не так» до конкретной подсистемы.</p>"}],
 ch:[
 {t:"c",d:1,q:"Пришёл алерт о заполнении диска. Покажи занятость всех разделов в человекочитаемом виде.",ans:["df -h","df -h /","df --human-readable"],hint:"disk free плюс human-readable",why:"df -h — первая команда при алерте на диск: она показывает, какой именно раздел переполнен. Дальше du ищет виновника внутри этого раздела."},
 {t:"c",d:2,q:"Нужно понять, с какими аргументами запущен nginx и от какого пользователя он работает. Найди его процессы в списке всех процессов системы.",ans:["ps aux | grep nginx","ps -ef | grep nginx","pgrep -a nginx","pgrep nginx"],hint:"ps aux и фильтрация через пайп",why:"Смотри на колонку с полной командой: она показывает аргументы запуска, путь к конфигу и часто сразу объясняет, что это за процесс и кто его запустил."},
 {t:"q",d:3,q:"На сервере с 3,8 ГБ памяти <code>free -h</code> показывает: free 0.4Gi, buff/cache 2.2Gi, available 2.4Gi. Коллега предлагает срочно заказать больше памяти. Что ответить?",a:["Согласиться, свободной памяти почти нет","Памяти достаточно: Linux занимает свободную память дисковым кэшем и освобождает её по требованию — смотреть надо на available (2,4 ГБ), а не на free","Нужно перезапустить сервисы, чтобы освободить память","Нужно очистить кэш командой sync"],c:1,why:"Незанятая память — это потраченная впустую память, поэтому ядро использует её под кэш. Реальный признак нехватки — низкий available вместе с ростом использования swap."},
 {t:"q",d:4,q:"Load average на 8-ядерном сервере равен 12.0, при этом в top суммарное потребление CPU процессами невысокое, а показатель wa (iowait) — 40%. О чём это говорит?",a:["Процессор перегружен вычислениями","Процессы massово ждут ввода-вывода: узкое место — диск или сетевое хранилище, а не CPU","Ошибка в измерениях uptime","Нужно добавить оперативной памяти"],c:1,why:"Load average учитывает и процессы в состоянии ожидания ввода-вывода. Расхождение «высокий LA — низкий CPU — высокий iowait» однозначно указывает на диск или сеть: добавление процессорных ядер здесь не поможет."},
 {t:"q",d:4,q:"Сервис перестал отвечать. Коллега сразу предлагает <code>kill -9</code>, чтобы не тратить время. Какой порядок правильный и почему это не формальность?",a:["Он прав, -9 быстрее и надёжнее","Сначала обычный kill (SIGTERM): процесс успеет сбросить буферы на диск, закрыть соединения с БД и завершить текущие запросы; -9 применяют, только если через несколько секунд процесс не завершился","kill вообще нельзя применять к сервисам","Нужно сразу перезагружать сервер"],c:1,why:"SIGKILL невозможно перехватить: процесс исчезает мгновенно, оставляя недописанные файлы и оборванные транзакции. Несколько секунд ожидания дешевле, чем повреждённые данные."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Алерт: раздел <code>/var</code> заполнен на 100%. Ты запускаешь <code>du -sh /var/*</code> — сумма около 6 ГБ при размере раздела 20 ГБ. <code>df -h</code> при этом упорно показывает 100%. Что происходит на уровне системы и какова последовательность действий?",a:["Файловая система повреждена, нужен fsck и перезагрузка","Процесс держит открытым удалённый файл: ядро не освобождает блоки, пока жив дескриптор. Найти через <code>lsof | grep deleted</code>, перезапустить этот сервис, проверить df ещё раз и настроить корректную ротацию логов","du показывает неверные данные, нужно доверять df и расширить диск","Не хватает прав, du нужно запускать от root"],c:1,why:"Расхождение df и du — точный диагностический признак этой ситуации. Обычно виновата ротация логов, при которой файл переименовали или удалили, а сервис не переоткрыл дескриптор — лечится опцией copytruncate или сигналом сервису после ротации."}]},

{id:"l5",t:"Bash-скрипты",goal:"Автоматизировать то, что делал руками, и не навредить",
 cards:[
 {h:"Скелет скрипта, который не навредит",b:"<pre>#!/bin/bash\nset -euo pipefail\n\nAPP_DIR=\"/opt/app\"\nLOG=\"/var/log/deploy.log\"\n\nif [ ! -d \"$APP_DIR\" ]; then\n  echo \"нет каталога $APP_DIR\" &gt;&amp;2\n  exit 1\nfi\n\nfor f in \"$APP_DIR\"/*.log; do\n  echo \"обрабатываю $f\"\ndone</pre><p><b>Первая строка — shebang.</b> Она указывает системе, какой интерпретатор запускать. Без неё скрипт выполнится оболочкой по умолчанию, и синтаксис bash может не сработать: на многих системах <code>/bin/sh</code> — это dash, где нет массивов и <code>[[ ]]</code>.</p><p><code>#!/usr/bin/env bash</code> — более переносимый вариант: он ищет bash в PATH, а не требует его по фиксированному пути.</p>"},
 {h:"set -euo pipefail: три слова, отличающие инженера",b:"<div class='tw'><table><tr><th>флаг</th><th>что делает</th><th>от какой беды спасает</th></tr><tr><td><code>-e</code></td><td>выход при первой же ошибке</td><td>скрипт продолжает работу после провала и делает следующий шаг на неверных данных</td></tr><tr><td><code>-u</code></td><td>ошибка при обращении к необъявленной переменной</td><td><code>rm -rf $DIR/</code> при пустой DIR превращается в <code>rm -rf /</code></td></tr><tr><td><code>-o pipefail</code></td><td>код возврата пайпа — это код первой упавшей команды</td><td><code>tar ... | gzip &gt; f</code> считается успешным, даже если tar упал</td></tr></table></div><p><b>Про pipefail подробнее.</b> По умолчанию код возврата конвейера — это код <i>последней</i> команды. Если <code>tar</code> упал, а <code>gzip</code> отработал нормально, скрипт решит, что всё хорошо. Именно так «успешно» создаются пустые бэкапы, о которых узнают в день восстановления.</p><p><b>Ограничение <code>-e</code>:</b> он не срабатывает внутри условий (<code>if команда; then</code>) и для команд перед <code>||</code>. Это не баг: там ненулевой код — часть логики, а не ошибка.</p>"},
 {h:"Код возврата — язык автоматизации",b:"<p>Каждая команда возвращает число: <b>0</b> — успех, любое другое — ошибка. Посмотреть последнее: <code>echo $?</code>.</p><p>На этом соглашении держится вся автоматизация: CI считает шаг упавшим по ненулевому коду, Docker HEALTHCHECK решает, здоров ли контейнер, systemd — перезапускать ли сервис.</p><pre>curl -fsS http://localhost/health || exit 1\n<span class='c'># || выполняется только при ненулевом коде</span>\n\nmkdir -p /opt/app &amp;&amp; cd /opt/app\n<span class='c'># &amp;&amp; выполняется только при успехе</span></pre><div class='tw'><table><tr><th>код</th><th>значение</th></tr><tr><td>0</td><td>успех</td></tr><tr><td>1</td><td>общая ошибка</td></tr><tr><td>2</td><td>неверное использование команды</td></tr><tr><td>126</td><td>файл найден, но не исполняемый</td></tr><tr><td>127</td><td>команда не найдена</td></tr><tr><td>130</td><td>прервано Ctrl+C</td></tr><tr><td>137</td><td>убито SIGKILL (часто OOM)</td></tr></table></div>"},
 {h:"Кавычки: главный источник разрушительных багов",b:"<pre>DIR=\"/opt/my app\"\nrm -rf $DIR      <span class='c'># ОПАСНО: две цели — /opt/my и app</span>\nrm -rf \"$DIR\"    <span class='c'># верно: один аргумент</span></pre><p><b>Что происходит без кавычек.</b> Оболочка разбивает значение переменной по пробелам и подставляет как несколько отдельных аргументов. Пробел в пути — и команда получает не то, что ты имел в виду. Пустая переменная — и аргумент просто исчезает.</p><p>Правило: <b>всегда заключай переменные в двойные кавычки</b>, кроме редких случаев, когда разбиение на слова нужно осознанно.</p><p>Двойные кавычки подставляют значения переменных, одинарные — нет:</p><pre>NAME=api\necho \"сервис $NAME\"   <span class='c'># сервис api</span>\necho 'сервис $NAME'   <span class='c'># сервис $NAME</span></pre>"},
 {h:"Условия и проверки",b:"<pre>[ -f \"$F\" ]      <span class='c'># обычный файл существует</span>\n[ -d \"$D\" ]      <span class='c'># каталог существует</span>\n[ -x \"$S\" ]      <span class='c'># файл исполняемый</span>\n[ -z \"$V\" ]      <span class='c'># переменная пустая</span>\n[ -n \"$V\" ]      <span class='c'># переменная непустая</span>\n[ \"$A\" = \"$B\" ]  <span class='c'># строки равны</span>\n[ \"$N\" -gt 10 ]  <span class='c'># число больше</span></pre><p><b>Проверка обязательных переменных в начале скрипта</b> экономит часы отладки: скрипт падает сразу и с внятным сообщением, а не на середине с непонятной ошибкой в чужой команде.</p><pre>: \"${TOKEN:?переменная TOKEN обязательна}\"\ntest -n \"$DB_HOST\" || { echo \"нет DB_HOST\" &gt;&amp;2; exit 1; }</pre><p>Первая форма компактна: если переменная пуста, скрипт завершится с указанным сообщением.</p>"},
 {h:"Куда писать сообщения и как не потерять ошибки",b:"<p>У процесса два выходных потока: <b>stdout</b> (полезный вывод) и <b>stderr</b> (ошибки и диагностика). Сообщения об ошибках пиши в stderr:</p><pre>echo \"не найден конфиг\" &gt;&amp;2</pre><p>Тогда полезный вывод скрипта можно направить в файл или передать следующей команде, а ошибки останутся видны человеку и не смешаются с данными.</p><p>Для журналирования удобна функция с меткой времени:</p><pre>log(){ echo \"[$(date +%F\\ %T)] $*\" &gt;&amp;2; }\nlog \"начинаю деплой версии $VERSION\"</pre><p>Отметка времени в логе скрипта — то, что превращает «скрипт что-то делал» в восстановимую хронологию инцидента.</p>"},
 {h:"Идемпотентность и защита от повторного запуска",b:"<p>Хороший скрипт можно запустить дважды без вреда. Приёмы:</p><pre>mkdir -p /opt/app          <span class='c'># не ошибка, если каталог есть</span>\nln -sfn \"$NEW\" /opt/current <span class='c'># перезаписать ссылку</span>\nrsync -a --delete src/ dst/ <span class='c'># привести к состоянию, а не «добавить»</span></pre><p>И защита от одновременного выполнения:</p><pre>flock -n /var/lock/deploy.lock \"$0\" || { echo \"уже выполняется\"; exit 0; }</pre><p>Без блокировки два одновременных запуска (например, из cron и руками) могут одновременно писать в один архив или деплоить разные версии.</p>"}],
 ch:[
 {t:"c",d:1,q:"Предыдущая команда завершилась, и ты хочешь понять, успешно ли. Как посмотреть её код возврата?",ans:["echo $?","echo \"$?\"","$?"],hint:"специальная переменная оболочки",why:"$? хранит код последней завершённой команды: 0 — успех, любое другое число — ошибка. Именно это значение читают CI, systemd и healthcheck, решая, что делать дальше."},
 {t:"f",d:2,q:"Ты пишешь скрипт деплоя. Допиши строку, которая заставит его падать при первой же ошибке, ругаться на необъявленные переменные и не терять ошибки внутри конвейеров.",pre:"#!/bin/bash\n___\n\nAPP_DIR=\"/opt/app\"",ans:["set -euo pipefail","set -eou pipefail","set -euo pipefail;"],hint:"одна строка: set с тремя опциями",why:"Без -e скрипт продолжит выполняться после провала и выполнит следующий шаг на неверных данных. Эти три опции — минимальная гигиена любого скрипта, который что-то меняет в системе."},
 {t:"q",d:3,q:"В скрипте есть строка <code>curl -fsS http://localhost:8080/health || exit 1</code>. Объясни, что именно она делает и зачем нужны флаги <code>-fsS</code>.",a:["Всегда завершает скрипт после проверки","Проверяет здоровье сервиса: -f заставляет curl вернуть ненулевой код при HTTP-ошибке, -s убирает индикатор прогресса, -S оставляет сообщения об ошибках; при неуспехе скрипт завершается с кодом 1","Просто выводит содержимое эндпоинта health","Перезапускает сервис при ошибке"],c:1,why:"Ключевая деталь — флаг -f: без него curl возвращает 0 даже при ответе 500, потому что запрос технически выполнен. Именно поэтому проверки здоровья без -f молча пропускают неработающий сервис."},
 {t:"q",d:4,q:"В скрипте очистки есть строка <code>rm -rf $BUILD_DIR/*</code>. Из-за опечатки в имени переменная <code>BUILD_DIR</code> оказалась пустой. Что произойдёт при запуске от root и какие две меры предотвратили бы это?",a:["Скрипт пропустит строку, ничего не произойдёт","Команда превратится в <code>rm -rf /*</code> и начнёт удалять систему; предотвращают <code>set -u</code> (скрипт упадёт на необъявленной переменной) и кавычки вокруг переменной","Будет синтаксическая ошибка","Удалится только текущий каталог"],c:1,why:"Пустая переменная без кавычек просто исчезает из команды, оставляя опасный шаблон. set -u прерывает выполнение до того, как что-либо произойдёт — это буквально защита от катастрофы ценой одной строки."},
 {t:"q",d:4,q:"Шаг пайплайна завершился с кодом 126. Что означает это число и какова наиболее вероятная причина?",a:["Команда не найдена — опечатка в имени","Файл найден, но не является исполняемым: у скрипта нет бита x, и это состояние хранится в git","Не хватило памяти","Скрипт прерван пользователем"],c:1,why:"126 — найден, но не исполняем; 127 — не найден. Бит выполнения хранится в git, поэтому исправление chmod +x нужно закоммитить (git update-index --chmod=+x), иначе каждый свежий клон будет падать так же."},
 {t:"q",d:5,q:"<b>Инцидент.</b> Ночной скрипт бэкапа полгода отрабатывал «успешно»: в логе нет ошибок, задание зелёное. При попытке восстановления выяснилось, что архивы пустые. В скрипте строка <code>tar czf - /var/lib/db | gzip &gt; /backup/db.gz</code>, флагов <code>set</code> нет. Что происходило и какие два вывода из этого следуют?",a:["Не хватало прав на каталог /backup","Код возврата конвейера — это код последней команды: tar падал, а gzip успешно записывал пустой поток, поэтому скрипт считал всё удачным. Выводы: нужен <code>set -o pipefail</code>, и успех бэкапа проверяется тестовым восстановлением, а не отсутствием ошибок в логе","Заканчивалось место на диске","Архив повреждался при передаче"],c:1,why:"Технический урок про pipefail здесь важен, но второй вывод важнее: любая проверка, основанная на «нет ошибок в логе», проверяет отсутствие сообщений, а не наличие результата. Бэкап считается работающим только после успешного восстановления из него."}]}
);
